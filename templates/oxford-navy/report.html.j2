<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oxford Intelligence Quarterly · {{ date_str }}</title>
    <style>
        /* ============================================================
         * OXFORD NAVY — British Academic Publication
         * ============================================================
         * Deep navy #002147 · White/gold text · Herringbone texture
         * Gold shield decoration · "Dominus Illuminatio Mea"
         * Bodleian Note cards · Scholarly serif · Chapter numbers
         * Gold-blue palette: elegant, authoritative, academic
         * ============================================================ */

        /* --- Design Tokens --- */
        :root {
            /* Oxford Blues */
            --oxford:           #002147;
            --oxford-light:     #0A3260;
            --oxford-mid:       #162D50;
            --oxford-dark:      #001530;
            --oxford-surface:   #0D2B4F;
            --oxford-dim:       rgba(0, 33, 71, 0.85);
            --oxford-card:      #0B2844;
            --oxford-inset:     #061E3A;

            /* Gold Accents */
            --gold:             #C7B265;
            --gold-light:       #D4BC6A;
            --gold-bright:      #E8D08C;
            --gold-warm:        #DFC97A;
            --gold-dim:         rgba(199, 178, 101, 0.15);
            --gold-faint:       rgba(199, 178, 101, 0.08);
            --gold-glow:        rgba(199, 178, 101, 0.25);
            --gold-border:      rgba(199, 178, 101, 0.35);
            --gold-rule:        rgba(199, 178, 101, 0.5);

            /* Text on Dark */
            --text-white:       #FFFFFF;
            --text-cream:       #F0EBD8;
            --text-light:       #CCC7B5;
            --text-muted:       #9A9585;
            --text-dim:         #706B5D;

            /* Priority */
            --pri-high:         #E8A04C;
            --pri-high-bg:      rgba(232, 160, 76, 0.12);
            --pri-mid:          #C7B265;
            --pri-mid-bg:       rgba(199, 178, 101, 0.10);
            --pri-low:          #7A9A8A;
            --pri-low-bg:       rgba(122, 154, 138, 0.08);

            /* Borders */
            --border-gold:      rgba(199, 178, 101, 0.30);
            --border-light:     rgba(255, 255, 255, 0.10);
            --border-medium:    rgba(255, 255, 255, 0.15);

            /* Typography — Scholarly Serif */
            --font-serif: "Georgia", "Palatino Linotype", "Times New Roman",
                          "Noto Serif SC", "Source Han Serif SC", "Songti SC", serif;
            --font-sans: "Optima", "Avenir Next", "Helvetica Neue",
                         "PingFang SC", "Noto Sans SC", sans-serif;
            --font-display: "Times New Roman", "Georgia",
                            "Noto Serif SC", serif;
            --font-mono: "Inconsolata", "Consolas", "SF Mono",
                         "Noto Sans SC", monospace;

            /* Radius */
            --radius-sm: 3px;
            --radius-md: 5px;
            --radius-lg: 8px;

            /* Shadows */
            --shadow-card:    0 2px 8px rgba(0, 0, 0, 0.30);
            --shadow-gold:    0 2px 12px rgba(199, 178, 101, 0.20);
            --shadow-deep:    0 8px 24px rgba(0, 0, 0, 0.40);
            --shadow-inset:   inset 0 1px 3px rgba(0, 0, 0, 0.25);
        }

        /* --- PDF Page Rules --- */
        @page {
            size: A4;
            margin: 2.5cm;
            @top-center {
                content: "Confidential · {{ date_str }}";
                font-family: "Optima", "Avenir Next", sans-serif;
                font-size: 7pt;
                color: #C7B265;
                letter-spacing: 0.15em;
                text-transform: uppercase;
            }
            @bottom-left {
                content: "Oxford Intelligence Quarterly";
                font-family: "Georgia", serif;
                font-size: 8pt;
                color: #9A9585;
                font-style: italic;
            }
            @bottom-right {
                content: "Page " counter(page);
                font-family: "Georgia", serif;
                font-size: 8pt;
                color: #9A9585;
            }
        }

        /* --- Reset --- */
        *, *::before, *::after {
            margin: 0; padding: 0; box-sizing: border-box;
        }

        /* --- Body: Deep navy + herringbone tweed texture --- */
        body {
            font-family: var(--font-serif);
            /* Herringbone pattern: crossing 45deg / -45deg fine lines */
            background:
                repeating-linear-gradient(
                    45deg,
                    transparent,
                    transparent 8px,
                    rgba(199, 178, 101, 0.018) 8px,
                    rgba(199, 178, 101, 0.018) 9px
                ),
                repeating-linear-gradient(
                    -45deg,
                    transparent,
                    transparent 8px,
                    rgba(255, 255, 255, 0.015) 8px,
                    rgba(255, 255, 255, 0.015) 9px
                ),
                repeating-linear-gradient(
                    45deg,
                    transparent,
                    transparent 16px,
                    rgba(199, 178, 101, 0.012) 16px,
                    rgba(199, 178, 101, 0.012) 17px
                ),
                repeating-linear-gradient(
                    -45deg,
                    transparent,
                    transparent 16px,
                    rgba(255, 255, 255, 0.010) 16px,
                    rgba(255, 255, 255, 0.010) 17px
                ),
                /* Vertical gradient: darker edges */
                linear-gradient(
                    180deg,
                    #001228 0%,
                    var(--oxford) 12%,
                    var(--oxford) 88%,
                    #001228 100%
                );
            color: var(--text-cream);
            font-size: 16px;
            line-height: 1.70;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        a {
            color: var(--gold);
            text-decoration: none;
            border-bottom: 1px solid var(--border-gold);
            transition: color 0.25s, border-color 0.25s;
        }
        a:hover {
            color: var(--gold-bright);
            border-bottom-color: var(--gold);
        }

        /* --- Document Container --- */
        .document {
            max-width: 780px;
            margin: 0 auto;
            padding: 64px 40px;
        }

        /* ============================================================
         * MASTHEAD — Oxford Crest + Shield + Gold Motto
         * ============================================================ */
        .masthead {
            text-align: center;
            padding: 72px 0 56px;
            margin-bottom: 56px;
            position: relative;
            /* Bottom gold rule */
            border-bottom: 2px solid var(--border-gold);
        }

        /* Top double gold rule */
        .masthead::before {
            content: '';
            position: absolute;
            top: 0; left: 8%; right: 8%;
            height: 3px;
            background: linear-gradient(90deg,
                transparent,
                var(--gold-dim) 15%,
                var(--gold) 50%,
                var(--gold-dim) 85%,
                transparent
            );
        }

        /* Top center gold diamond ornament */
        .masthead::after {
            content: '';
            position: absolute;
            top: -5px;
            left: 50%;
            width: 10px;
            height: 10px;
            border: 1.5px solid var(--gold);
            transform: translateX(-50%) rotate(45deg);
            background: var(--oxford);
        }

        /* Shield (CSS only — rounded bottom, gold border) */
        .shield {
            display: inline-block;
            width: 84px;
            height: 96px;
            border: 2px solid var(--gold);
            border-radius: var(--radius-md) var(--radius-md) 50% 50%;
            position: relative;
            margin-bottom: 20px;
            background: linear-gradient(
                180deg,
                var(--oxford-surface) 0%,
                var(--oxford-dark) 100%
            );
            box-shadow: var(--shadow-gold);
        }
        .shield::before {
            content: '◆';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 2.2em;
            color: var(--gold);
        }
        .shield::after {
            content: '';
            position: absolute;
            inset: 5px;
            border: 1px solid var(--gold-dim);
            border-radius: 2px 2px 50% 50%;
        }

        /* Motto */
        .motto {
            font-family: var(--font-serif);
            font-size: 0.72em;
            font-style: italic;
            color: var(--gold);
            letter-spacing: 0.18em;
            margin-bottom: 8px;
            text-transform: uppercase;
        }

        .university-name {
            font-family: var(--font-sans);
            font-size: 0.76em;
            font-weight: 600;
            color: var(--gold-light);
            letter-spacing: 0.40em;
            text-transform: uppercase;
            margin-bottom: 4px;
        }

        .pub-title {
            font-family: var(--font-display);
            font-size: 2.8em;
            font-weight: 300;
            color: var(--text-white);
            line-height: 1.10;
            letter-spacing: -0.02em;
            margin-bottom: 6px;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.30);
        }

        .pub-subtitle {
            font-family: var(--font-serif);
            font-size: 1.0em;
            font-style: italic;
            color: var(--text-light);
            line-height: 1.60;
            max-width: 560px;
            margin: 0 auto 36px;
        }

        /* Edition grid */
        .edition-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 20px;
            font-family: var(--font-sans);
            font-size: 0.78em;
            color: var(--text-muted);
            max-width: 520px;
            margin: 0 auto;
            padding-top: 20px;
            border-top: 1px solid var(--border-light);
        }

        .edition-item { text-align: center; }

        .edition-label {
            display: block;
            font-weight: 700;
            color: var(--gold);
            letter-spacing: 0.10em;
            text-transform: uppercase;
            font-size: 0.85em;
            margin-bottom: 3px;
        }

        /* ============================================================
         * TABLE OF CONTENTS — Gold bordered index
         * ============================================================ */
        .contents {
            background:
                linear-gradient(135deg, var(--gold-faint) 0%, transparent 50%),
                var(--oxford-surface);
            border: 1px solid var(--border-gold);
            border-radius: var(--radius-md);
            padding: 36px 32px;
            margin-bottom: 56px;
            position: relative;
            box-shadow: var(--shadow-card);
        }

        /* Gold gradient top accent */
        .contents::before {
            content: '';
            position: absolute;
            top: -1px; left: 24px; right: 24px;
            height: 2px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
        }

        /* Bottom-right corner ornament */
        .contents::after {
            content: '';
            position: absolute;
            bottom: 0; right: 0;
            width: 20px; height: 20px;
            border-bottom: 2px solid var(--gold-dim);
            border-right: 2px solid var(--gold-dim);
            border-radius: 0 0 var(--radius-md) 0;
        }

        .contents-title {
            font-family: var(--font-sans);
            font-size: 0.80em;
            color: var(--gold);
            text-align: center;
            margin-bottom: 20px;
            letter-spacing: 0.18em;
            text-transform: uppercase;
            font-weight: 600;
        }

        .contents-divider {
            width: 50px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold-rule), transparent);
            margin: 0 auto 20px;
        }

        .contents-item {
            display: flex;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid var(--border-light);
            font-size: 0.92em;
            transition: color 0.2s;
        }

        .contents-item:last-child { border-bottom: none; }
        .contents-item:hover .contents-section { color: var(--gold); }

        .contents-section {
            color: var(--text-light);
            flex-shrink: 0;
            transition: color 0.2s;
        }

        .contents-leader {
            flex: 1;
            border-bottom: 1px dotted var(--border-light);
            margin: 0 16px;
        }

        .contents-num {
            font-family: var(--font-mono);
            color: var(--gold);
            font-weight: 600;
            font-size: 0.92em;
        }

        /* ============================================================
         * EXECUTIVE ABSTRACT — Gold-framed panel with ornaments
         * ============================================================ */
        .abstract {
            background:
                linear-gradient(135deg, var(--gold-faint) 0%, transparent 40%),
                linear-gradient(315deg, var(--gold-faint) 0%, transparent 40%),
                var(--oxford-surface);
            border: 1px solid var(--border-gold);
            border-radius: var(--radius-md);
            padding: 40px 32px;
            margin-bottom: 48px;
            position: relative;
            page-break-inside: avoid;
            box-shadow: var(--shadow-gold);
        }

        /* Top gold ornamental line */
        .abstract::before {
            content: '';
            position: absolute;
            top: -1px; left: 32px; right: 32px;
            height: 2px;
            background: linear-gradient(90deg, var(--gold), var(--gold-bright), var(--gold));
        }

        /* Bottom-right corner ornament */
        .abstract::after {
            content: '';
            position: absolute;
            bottom: 0; right: 0;
            width: 28px; height: 28px;
            border-bottom: 2px solid var(--gold);
            border-right: 2px solid var(--gold);
            opacity: 0.35;
            border-radius: 0 0 var(--radius-md) 0;
        }

        .abstract-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .abstract-label {
            font-family: var(--font-sans);
            font-size: 0.78em;
            color: var(--gold);
            letter-spacing: 0.18em;
            text-transform: uppercase;
            font-weight: 600;
        }

        .abstract-ornament {
            display: block;
            color: var(--gold);
            font-size: 1.3em;
            margin-top: 8px;
        }

        .abstract-rule {
            width: 60px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold-rule), transparent);
            margin: 0 auto 20px;
        }

        .abstract p {
            font-size: 1.0em;
            line-height: 1.80;
            color: var(--text-cream);
            margin-bottom: 16px;
            text-align: justify;
        }

        .abstract p:first-of-type::first-letter {
            font-size: 2.4em;
            font-weight: 700;
            color: var(--gold);
            float: left;
            line-height: 0.82;
            margin-right: 5px;
            margin-top: 5px;
            text-shadow: 0 1px 4px rgba(199, 178, 101, 0.30);
        }

        .abstract p:last-of-type { margin-bottom: 0; }

        .abstract p strong {
            color: var(--gold-bright);
            font-weight: 700;
        }

        /* ============================================================
         * BODLEIAN NOTE — Special card with book icon
         * ============================================================ */
        .bodleian-note {
            background:
                linear-gradient(90deg, var(--gold-faint) 0%, transparent 30%),
                var(--oxford-card);
            border: 1px solid var(--gold-dim);
            border-left: 3px solid var(--gold);
            border-radius: 0 var(--radius-md) var(--radius-md) 0;
            padding: 24px;
            margin-bottom: 48px;
            position: relative;
            box-shadow: var(--shadow-card);
        }

        .bodleian-label {
            font-family: var(--font-sans);
            font-size: 0.68em;
            font-weight: 700;
            color: var(--gold);
            letter-spacing: 0.14em;
            text-transform: uppercase;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .bodleian-label::before {
            content: '📖';
            font-size: 1.2em;
        }

        .bodleian-note p {
            font-size: 0.90em;
            line-height: 1.70;
            color: var(--text-light);
            font-style: italic;
        }

        /* ============================================================
         * STATISTICS PANEL — Data overview
         * ============================================================ */
        .stats-panel {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 16px;
            margin-bottom: 56px;
        }

        .stat-card {
            background:
                linear-gradient(135deg, var(--gold-faint), transparent),
                var(--oxford-card);
            border: 1px solid var(--border-gold);
            border-radius: var(--radius-md);
            padding: 20px 16px;
            text-align: center;
            box-shadow: var(--shadow-card);
            transition: border-color 0.25s, box-shadow 0.25s;
        }
        .stat-card:hover {
            border-color: var(--gold);
            box-shadow: var(--shadow-gold);
        }

        .stat-value {
            font-family: var(--font-display);
            font-size: 2.0em;
            font-weight: 300;
            color: var(--gold);
            line-height: 1.1;
            margin-bottom: 4px;
        }

        .stat-label {
            font-family: var(--font-sans);
            font-size: 0.70em;
            color: var(--text-muted);
            letter-spacing: 0.10em;
            text-transform: uppercase;
        }

        /* ============================================================
         * SECTION — Chapter headers with gold numbering
         * ============================================================ */
        .section {
            margin-bottom: 56px;
        }

        .section-header {
            text-align: center;
            padding: 32px 24px;
            margin-bottom: 32px;
            position: relative;
            /* Gold tint gradient */
            background:
                linear-gradient(180deg, var(--gold-faint) 0%, transparent 100%),
                var(--oxford-surface);
            border-top: 1px solid var(--border-gold);
            border-bottom: 1px solid var(--border-gold);
            border-radius: var(--radius-sm);
        }

        /* Gold diamond accent at top */
        .section-header::before {
            content: '◆';
            position: absolute;
            top: -9px; left: 50%;
            transform: translateX(-50%);
            color: var(--gold);
            font-size: 0.55em;
            background: var(--oxford);
            padding: 0 12px;
        }

        /* Bottom ornament */
        .section-header::after {
            content: '— ✦ —';
            position: absolute;
            bottom: -10px; left: 50%;
            transform: translateX(-50%);
            font-size: 0.60em;
            color: var(--gold-dim);
            background: var(--oxford);
            padding: 0 16px;
            letter-spacing: 0.3em;
        }

        .chapter-number {
            font-family: var(--font-sans);
            font-size: 0.68em;
            color: var(--gold);
            letter-spacing: 0.22em;
            text-transform: uppercase;
            font-weight: 600;
            margin-bottom: 6px;
        }

        .chapter-title-row {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 14px;
            margin-bottom: 10px;
        }

        .chapter-icon {
            font-size: 1.5em;
        }

        .chapter-title {
            font-family: var(--font-display);
            font-size: 1.9em;
            font-weight: 400;
            color: var(--text-white);
            letter-spacing: -0.01em;
        }

        .chapter-meta {
            font-family: var(--font-sans);
            font-size: 0.74em;
            color: var(--text-muted);
            font-style: italic;
        }

        /* Section color bar (left edge) */
        .section-color-bar {
            position: absolute;
            left: 0; top: 0; bottom: 0;
            width: 4px;
            border-radius: var(--radius-sm) 0 0 var(--radius-sm);
        }

        /* ============================================================
         * ARTICLES — Scholarly brief cards
         * ============================================================ */
        .article {
            padding: 24px 0;
            border-bottom: 1px solid var(--border-light);
            page-break-inside: avoid;
            position: relative;
        }

        .article:last-child { border-bottom: none; }

        /* Priority indicators */
        .article.must-read {
            border-left: 3px solid var(--pri-high);
            padding-left: 20px;
            background: linear-gradient(90deg, var(--pri-high-bg) 0%, transparent 30%);
        }

        .article.recommended {
            border-left: 3px solid var(--pri-mid);
            padding-left: 20px;
            background: linear-gradient(90deg, var(--pri-mid-bg) 0%, transparent 25%);
        }

        .article.fyi {
            border-left: 3px solid var(--pri-low);
            padding-left: 20px;
            background: linear-gradient(90deg, var(--pri-low-bg) 0%, transparent 18%);
        }

        /* Article number */
        .article-index {
            font-family: var(--font-mono);
            font-size: 0.62em;
            color: var(--text-dim);
            letter-spacing: 0.08em;
            margin-bottom: 6px;
        }

        .article-title {
            font-family: var(--font-serif);
            font-size: 1.22em;
            font-weight: 600;
            line-height: 1.40;
            margin-bottom: 10px;
            color: var(--text-white);
        }

        .article-title a {
            color: inherit;
            border-bottom: none;
        }
        .article-title a:hover {
            color: var(--gold);
        }

        .article.must-read .article-title a {
            color: var(--gold-bright);
            font-weight: 700;
        }

        /* Impact badge */
        .impact-label {
            display: inline-flex;
            font-family: var(--font-sans);
            font-size: 0.56em;
            font-weight: 700;
            padding: 2px 8px;
            border-radius: var(--radius-sm);
            text-transform: uppercase;
            letter-spacing: 0.08em;
            margin-left: 8px;
            vertical-align: middle;
        }

        .article.must-read .impact-label {
            background: var(--gold);
            color: var(--oxford);
        }

        .article.recommended .impact-label {
            background: var(--gold-dim);
            color: var(--gold);
            border: 1px solid var(--border-gold);
        }

        /* Attribution */
        .article-attribution {
            display: flex;
            align-items: center;
            gap: 14px;
            margin-bottom: 12px;
            flex-wrap: wrap;
        }

        .article-source {
            font-family: var(--font-serif);
            font-size: 0.84em;
            font-style: italic;
            color: var(--gold);
        }

        .article-divider {
            color: var(--border-light);
            font-size: 0.70em;
        }

        .article-tags {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }

        .article-tag {
            font-family: var(--font-mono);
            font-size: 0.64em;
            padding: 2px 7px;
            background: var(--gold-faint);
            color: var(--text-light);
            border: 1px solid var(--border-gold);
            border-radius: var(--radius-sm);
            text-transform: uppercase;
            letter-spacing: 0.04em;
            transition: border-color 0.2s, color 0.2s;
        }
        .article-tag:hover {
            border-color: var(--gold);
            color: var(--gold);
        }

        /* Article body — white serif on navy */
        .article-body {
            font-size: 0.94em;
            line-height: 1.78;
            color: var(--text-light);
            text-align: justify;
        }

        .article-body strong {
            color: var(--text-cream);
            font-weight: 700;
        }

        .article-body em {
            color: var(--gold-light);
        }

        .article-body code {
            font-family: var(--font-mono);
            font-size: 0.85em;
            background: var(--oxford-surface);
            color: var(--gold);
            padding: 2px 5px;
            border-radius: var(--radius-sm);
            border: 1px solid var(--border-light);
        }

        /* ============================================================
         * QUOTED BOX — Gold-bordered scholarly quote
         * ============================================================ */
        .quote-box {
            border: 1px solid var(--border-gold);
            border-left: 3px solid var(--gold);
            border-radius: 0 var(--radius-md) var(--radius-md) 0;
            padding: 24px;
            margin: 28px 0;
            background:
                linear-gradient(135deg, var(--gold-faint), transparent 60%),
                var(--oxford-card);
            position: relative;
            box-shadow: var(--shadow-card);
        }

        .quote-box::before {
            content: '"';
            position: absolute;
            top: -6px; left: 16px;
            font-family: var(--font-display);
            font-size: 2.8em;
            color: var(--gold);
            line-height: 1;
        }

        .quote-box p {
            font-style: italic;
            color: var(--text-cream);
            line-height: 1.70;
            padding-left: 16px;
        }

        /* ============================================================
         * SECTION SEPARATOR — Gold rule with diamond
         * ============================================================ */
        .section-sep {
            text-align: center;
            padding: 28px 0;
            position: relative;
        }

        .section-sep::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 15%;
            right: 15%;
            height: 1px;
            background: linear-gradient(90deg,
                transparent,
                var(--border-gold) 20%,
                var(--gold-rule) 50%,
                var(--border-gold) 80%,
                transparent
            );
        }

        .section-sep-diamond {
            display: inline-block;
            position: relative;
            z-index: 1;
            background: var(--oxford);
            padding: 0 16px;
            color: var(--gold);
            font-size: 0.70em;
            letter-spacing: 0.5em;
        }

        /* ============================================================
         * COLOPHON / FOOTER — Oxford University Press style
         * ============================================================ */
        .colophon {
            margin-top: 72px;
            padding-top: 40px;
            border-top: 2px solid var(--border-gold);
            text-align: center;
            position: relative;
        }

        .colophon::before {
            content: '';
            position: absolute;
            top: -6px; left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
        }

        /* Gold diamond at top of footer */
        .colophon::after {
            content: '';
            position: absolute;
            top: -4px; left: 50%;
            width: 8px; height: 8px;
            border: 1px solid var(--gold);
            transform: translateX(-50%) rotate(45deg);
            background: var(--oxford);
        }

        .colophon-shield {
            font-size: 2.0em;
            margin-bottom: 16px;
            color: var(--gold);
        }

        .colophon-imprint {
            font-family: var(--font-sans);
            font-size: 0.72em;
            color: var(--gold);
            letter-spacing: 0.20em;
            text-transform: uppercase;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .colophon-main {
            font-family: var(--font-serif);
            font-size: 0.92em;
            color: var(--text-light);
            line-height: 1.60;
            margin-bottom: 12px;
        }

        .colophon-rule {
            width: 50px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--gold), transparent);
            margin: 16px auto;
        }

        .colophon-details {
            font-family: var(--font-sans);
            font-size: 0.74em;
            color: var(--text-dim);
            letter-spacing: 0.05em;
        }

        .colophon-motto {
            margin-top: 24px;
            font-family: var(--font-serif);
            font-style: italic;
            font-size: 0.88em;
            color: var(--gold);
        }

        .colophon-disclaimer {
            margin-top: 16px;
            font-family: var(--font-sans);
            font-size: 0.65em;
            color: var(--text-dim);
            line-height: 1.50;
            max-width: 480px;
            margin-left: auto;
            margin-right: auto;
        }

        /* ============================================================
         * PRINT MEDIA
         * ============================================================ */
        @media print {
            body {
                background: #002147 !important;
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }
            .document {
                max-width: 100%;
                padding: 0;
                box-shadow: none;
            }
            .masthead {
                padding: 36px 0;
                margin-bottom: 36px;
            }
            .section { margin-bottom: 36px; }
            .article { page-break-inside: avoid; }
            .stat-card { box-shadow: none; }
            a { border-bottom: none !important; }
        }

        /* ============================================================
         * MOBILE RESPONSIVE
         * ============================================================ */
        @media (max-width: 768px) {
            .document { padding: 32px 20px; }
            .masthead { padding: 40px 0 32px; }
            .pub-title { font-size: 2.0em; }
            .edition-grid { grid-template-columns: repeat(2, 1fr); }
            .contents { padding: 24px; }
            .abstract { padding: 24px; }
            .abstract p:first-of-type::first-letter {
                font-size: inherit; float: none; margin: 0;
            }
            .chapter-title { font-size: 1.4em; }
            .article-title { font-size: 1.05em; }
            .article-body { text-align: left; }
            .article-attribution {
                flex-direction: column;
                gap: 6px;
            }
            .stats-panel { grid-template-columns: repeat(2, 1fr); }
        }

        @media (max-width: 480px) {
            .document { padding: 24px 16px; }
            .pub-title { font-size: 1.6em; }
            .shield { width: 60px; height: 70px; }
            .shield::before { font-size: 1.6em; }
            .edition-grid { grid-template-columns: 1fr; }
            .chapter-title-row {
                flex-direction: column;
                gap: 4px;
            }
            .stats-panel { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="document">

        <!-- ====== MASTHEAD ====== -->
        <header class="masthead">
            <div class="shield"></div>
            <div class="motto">Dominus Illuminatio Mea</div>
            <div class="university-name">University of Oxford</div>
            <h1 class="pub-title">Intelligence Quarterly</h1>
            <div class="pub-subtitle">
                Scholarly analysis of contemporary developments in technology,
                science, and global affairs
            </div>
            <div class="edition-grid">
                <div class="edition-item">
                    <span class="edition-label">Edition</span>
                    <span>{{ date_str }}</span>
                </div>
                <div class="edition-item">
                    <span class="edition-label">Articles</span>
                    <span>{{ total_items }}</span>
                </div>
                {% set crit_ns = namespace(count=0) %}
                {% for section_key, items in briefs.items() %}
                {% if items is iterable and items is not string %}
                {% for item in items %}
                {% if item.priority == '🔴' %}{% set crit_ns.count = crit_ns.count + 1 %}{% endif %}
                {% endfor %}
                {% endif %}
                {% endfor %}
                {% if crit_ns.count > 0 %}
                <div class="edition-item">
                    <span class="edition-label">Urgent</span>
                    <span>{{ crit_ns.count }}</span>
                </div>
                {% endif %}
                <div class="edition-item">
                    <span class="edition-label">Status</span>
                    <span>Confidential</span>
                </div>
            </div>
        </header>

        <!-- ====== CONTENTS ====== -->
        <nav class="contents">
            <h2 class="contents-title">Table of Contents</h2>
            <div class="contents-divider"></div>
            {% set toc_ns = namespace(num=1) %}
            {% for section_key in section_order %}
            {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
            {% set meta = section_configs.get(section_key, {}) %}
            <div class="contents-item">
                <span class="contents-section">
                    <span style="color: {{ meta.color|default('#C7B265') }}; margin-right: 6px;">{{ meta.emoji|default('§') }}</span>
                    Chapter {{ toc_ns.num }}. {{ meta.title|default(section_key) }}
                </span>
                <span class="contents-leader"></span>
                <span class="contents-num">{{ briefs[section_key]|length }}</span>
            </div>
            {% set toc_ns.num = toc_ns.num + 1 %}
            {% endif %}
            {% endfor %}
        </nav>

        <!-- ====== EXECUTIVE ABSTRACT ====== -->
        {% if executive_summary %}
        <div class="abstract">
            <div class="abstract-header">
                <div class="abstract-label">Executive Abstract</div>
                <span class="abstract-ornament">⚜</span>
            </div>
            <div class="abstract-rule"></div>
            {% for line in executive_summary.strip().split('\n') %}
            {% if line.strip() %}
            <p>{{ line.strip() | md_inline }}</p>
            {% endif %}
            {% endfor %}
        </div>

        <!-- ====== BODLEIAN NOTE ====== -->
        <div class="bodleian-note">
            <div class="bodleian-label">Bodleian Note</div>
            <p>This intelligence brief compiles {{ total_items }} sources across
            {{ section_order|length }} domains of scholarly interest. Items marked
            with gold emphasis warrant immediate review by senior fellows. All
            findings are synthesised for clarity whilst preserving analytical rigour.</p>
        </div>
        {% endif %}

        <!-- ====== STATISTICS ====== -->
        {% if stats %}
        <div class="stats-panel">
            <div class="stat-card">
                <div class="stat-value">{{ total_items }}</div>
                <div class="stat-label">Total Sources</div>
            </div>
            <div class="stat-card">
                <div class="stat-value">{{ section_order|length }}</div>
                <div class="stat-label">Domains</div>
            </div>
            {% if stats.high_priority is defined %}
            <div class="stat-card">
                <div class="stat-value">{{ stats.high_priority }}</div>
                <div class="stat-label">High Priority</div>
            </div>
            {% endif %}
            {% if stats.sources is defined %}
            <div class="stat-card">
                <div class="stat-value">{{ stats.sources }}</div>
                <div class="stat-label">Unique Sources</div>
            </div>
            {% endif %}
        </div>
        {% endif %}

        <!-- ====== SECTIONS ====== -->
        {% set ch_ns = namespace(num=1) %}
        {% for section_key in section_order %}
        {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
        {% set meta = section_configs.get(section_key, {}) %}
        {% set section_color = meta.color|default('#C7B265') %}
        {% set items = briefs[section_key] %}

        {% if ch_ns.num > 1 %}
        <div class="section-sep">
            <span class="section-sep-diamond">◆ ◆ ◆</span>
        </div>
        {% endif %}

        <section class="section">
            <div class="section-header" style="border-top-color: {{ section_color }};">
                <span class="section-color-bar" style="background: {{ section_color }};"></span>
                <div class="chapter-number" style="color: {{ section_color }};">
                    Chapter {{ ch_ns.num }}
                </div>
                <div class="chapter-title-row">
                    <span class="chapter-icon">{{ meta.emoji|default('§') }}</span>
                    <h2 class="chapter-title">{{ meta.title|default(section_key) }}</h2>
                </div>
                <div class="chapter-meta">
                    {{ items|length }} article{{ 's' if items|length > 1 else '' }} for scholarly review
                </div>
            </div>

            {% for item in items %}
            {% set priority = item.priority|default('🟢') %}
            {% set p_class = 'must-read' if priority == '🔴' else ('recommended' if priority == '🟡' else 'fyi') %}
            <article class="article {{ p_class }}">
                <div class="article-index">§ {{ ch_ns.num }}.{{ loop.index }}</div>
                <h3 class="article-title">
                    <a href="{{ item.url|default('#') }}" target="_blank">{{ item.headline|default(item.title|default('Untitled')) }}</a>
                    {% if priority == '🔴' %}
                    <span class="impact-label">High Impact</span>
                    {% elif priority == '🟡' %}
                    <span class="impact-label">Notable</span>
                    {% endif %}
                </h3>
                <div class="article-attribution">
                    {% if item.source %}
                    <span class="article-source">{{ item.source }}</span>
                    {% endif %}
                    {% if item.tags and item.source %}
                    <span class="article-divider">|</span>
                    {% endif %}
                    {% if item.tags %}
                    <div class="article-tags">
                        {% for tag in item.tags %}
                        <span class="article-tag" style="border-color: {{ section_color }}40;">{{ tag }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
                {% if item.detail %}
                <div class="article-body">{{ item.detail | md_inline }}</div>
                {% endif %}
            </article>
            {% endfor %}
        </section>
        {% set ch_ns.num = ch_ns.num + 1 %}
        {% endif %}
        {% endfor %}

        <!-- ====== COLOPHON ====== -->
        <footer class="colophon">
            <div class="colophon-shield">◆</div>
            <div class="colophon-imprint">Oxford University Press</div>
            <div class="colophon-main">
                Published by the Oxford Intelligence Division<br>
                Department of Contemporary Studies
            </div>
            <div class="colophon-rule"></div>
            <div class="colophon-details">
                {{ date_str }} · {{ total_items }} sources analysed ·
                Generated {{ generated_time }} · Oxford, England
            </div>
            <div class="colophon-motto">
                <em>"Dominus illuminatio mea"</em>
            </div>
            <div class="colophon-disclaimer">
                This publication is prepared by Newsloom AI intelligence system.
                All analysis is for informational purposes only.
                © {{ date_str[:4] }} Oxford Intelligence Quarterly · All rights reserved.
            </div>
        </footer>

    </div>
</body>
</html>
