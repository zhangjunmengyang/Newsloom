<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>知识日报 · {{ date_str }} · Newsloom</title>
    <style>
        /* ============================================================
         * EDUCATION WARM — University Textbook Visual Narrative
         *
         * Warm cream base (#FFF8F0) · Paper texture (CSS gradients)
         * Chapter numbering (Chapter 1, 2, 3...)
         * Notebook ruled-line background (repeating-linear-gradient)
         * Yellow highlighter effect for must-read items
         * "Key Concept" warm gradient boxes
         * Side margin-note column effect
         * Pencil icon (✏️) decorations throughout
         *
         * Zero external dependencies · Inline CSS only
         * CJK font stack · PDF print media queries
         * ============================================================ */

        /* --- PDF Page Setup --- */
        @page {
            size: A4;
            margin: 2cm 1.5cm 2.2cm 1.5cm;
            @top-left {
                content: "✏️ Newsloom 知识日报";
                font-family: -apple-system, "PingFang SC", sans-serif;
                font-size: 7pt;
                color: #B8A084;
            }
            @top-right {
                content: "Chapter Notes · " attr(data-date);
                font-family: -apple-system, "PingFang SC", sans-serif;
                font-size: 7pt;
                color: #B8A084;
            }
            @bottom-center {
                content: "— 第 " counter(page) " 页 —";
                font-family: Georgia, "Songti SC", serif;
                font-size: 8pt;
                color: #8B7355;
            }
        }

        /* --- Reset --- */
        *, *::before, *::after {
            margin: 0; padding: 0; box-sizing: border-box;
        }

        /* --- Body: Notebook lined background + cream paper --- */
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
                         "PingFang SC", "Noto Sans CJK SC", "Microsoft YaHei",
                         "Helvetica Neue", Arial, sans-serif;
            /* Notebook ruled lines + warm paper texture */
            background:
                /* Ruled horizontal lines */
                repeating-linear-gradient(
                    0deg,
                    transparent,
                    transparent 31px,
                    rgba(180, 160, 130, 0.10) 31px,
                    rgba(180, 160, 130, 0.10) 32px
                ),
                /* Red margin line (left) */
                linear-gradient(
                    90deg,
                    transparent 59px,
                    rgba(220, 80, 80, 0.12) 59px,
                    rgba(220, 80, 80, 0.12) 60px,
                    transparent 60px
                ),
                /* Paper texture gradient */
                linear-gradient(
                    135deg,
                    #FFF8F0 0%,
                    #FFFDF7 20%,
                    #FEF5E7 40%,
                    #FFF8F0 60%,
                    #FFFDF7 80%,
                    #FFF3E0 100%
                );
            background-attachment: local;
            color: #2D1B0E;
            font-size: 15px;
            line-height: 1.72;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            min-height: 100vh;
        }

        a {
            color: #00695C;
            text-decoration: none;
            transition: all 0.2s ease;
        }
        a:hover {
            color: #E65100;
            text-decoration: underline;
            text-decoration-style: wavy;
            text-underline-offset: 3px;
        }

        /* --- Container --- */
        .container {
            max-width: 820px;
            margin: 0 auto;
            padding: 32px 24px 48px;
            /* Offset for margin line */
            padding-left: 72px;
            position: relative;
        }

        /* ============================================================
         * HEADER: Textbook cover
         * ============================================================ */
        .textbook-cover {
            background:
                /* Subtle cloth texture */
                repeating-linear-gradient(
                    45deg,
                    transparent,
                    transparent 8px,
                    rgba(255,255,255,0.03) 8px,
                    rgba(255,255,255,0.03) 9px
                ),
                linear-gradient(155deg,
                    #8B4513 0%,
                    #A0522D 15%,
                    #6B3410 35%,
                    #7B3F15 55%,
                    #8B4513 75%,
                    #5C2D0E 100%);
            color: #FFFFFF;
            padding: 52px 44px 44px;
            margin: 0 -72px 36px -72px;
            border-radius: 4px 4px 0 0;
            position: relative;
            overflow: hidden;
            box-shadow:
                0 8px 32px rgba(92, 45, 14, 0.25),
                inset 0 1px 0 rgba(255,255,255,0.08);
        }

        /* Book spine shadow */
        .textbook-cover::before {
            content: '';
            position: absolute;
            top: 0; left: 0; bottom: 0;
            width: 20px;
            background: linear-gradient(90deg,
                rgba(0,0,0,0.25) 0%,
                rgba(0,0,0,0.08) 50%,
                transparent 100%);
        }

        /* Gold edge foil */
        .textbook-cover::after {
            content: '';
            position: absolute;
            bottom: 0; left: 20px; right: 0;
            height: 4px;
            background: linear-gradient(90deg,
                #C4A962, #D4BC7A, #C4A962, #B89A52, #C4A962);
        }

        .cover-inner {
            position: relative;
            z-index: 1;
            padding-left: 24px;
        }

        .cover-edition {
            font-size: 0.68em;
            font-weight: 700;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            color: rgba(255,255,255,0.45);
            margin-bottom: 8px;
        }

        .cover-title {
            font-family: Georgia, "Noto Serif SC", "Source Han Serif SC", "Songti SC", serif;
            font-size: 2.8em;
            font-weight: 700;
            line-height: 1.12;
            margin-bottom: 8px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.2);
        }

        .cover-subtitle {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 1.1em;
            font-weight: 400;
            font-style: italic;
            opacity: 0.72;
            margin-bottom: 28px;
        }

        .cover-pencil-rule {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 24px;
        }

        .cover-pencil-rule-line {
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.25), transparent);
        }

        .cover-pencil-icon {
            font-size: 0.9em;
            opacity: 0.6;
        }

        .cover-meta-row {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .cover-pill {
            background: rgba(255,255,255,0.12);
            border: 1px solid rgba(255,255,255,0.18);
            padding: 6px 14px;
            border-radius: 100px;
            font-size: 0.8em;
            font-weight: 600;
            backdrop-filter: blur(4px);
        }

        .cover-pill-emoji {
            margin-right: 4px;
        }

        /* ============================================================
         * TABLE OF CONTENTS (condensed)
         * ============================================================ */
        .toc {
            background: #FFFFFF;
            border: 2px solid rgba(139, 115, 85, 0.18);
            border-radius: 12px;
            padding: 24px 28px;
            margin-bottom: 36px;
            box-shadow: 0 2px 8px rgba(45, 27, 14, 0.05);
            position: relative;
        }

        .toc::before {
            content: '📑';
            position: absolute;
            top: -14px;
            left: 24px;
            font-size: 1.3em;
            background: #FFF8F0;
            padding: 0 8px;
        }

        .toc-title {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 1.05em;
            font-weight: 700;
            color: #8B4513;
            margin-bottom: 14px;
            padding-bottom: 10px;
            border-bottom: 2px dotted rgba(139, 115, 85, 0.2);
        }

        .toc-list {
            list-style: none;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 6px 20px;
        }

        .toc-item {
            font-size: 0.88em;
            display: flex;
            align-items: center;
            gap: 8px;
            color: #5C4033;
            padding: 4px 0;
        }

        .toc-chapter-num {
            font-family: Georgia, serif;
            font-weight: 700;
            color: #8B4513;
            min-width: 18px;
        }

        .toc-item-title {
            flex: 1;
        }

        .toc-item-count {
            font-size: 0.82em;
            color: #B8A084;
            font-weight: 600;
        }

        .toc-dot-leader {
            flex: 1;
            border-bottom: 1px dotted rgba(139, 115, 85, 0.25);
            margin: 0 4px;
            min-width: 20px;
        }

        /* ============================================================
         * EXECUTIVE SUMMARY: Key Concept Box
         * ============================================================ */
        .key-concept-main {
            background:
                linear-gradient(135deg,
                    #FFF8E1 0%,
                    #FFECB3 30%,
                    #FFE082 60%,
                    #FFD54F 100%);
            border: 2px solid rgba(255, 160, 0, 0.3);
            border-radius: 14px;
            padding: 32px 32px 28px;
            margin-bottom: 40px;
            position: relative;
            page-break-inside: avoid;
            box-shadow:
                0 4px 16px rgba(255, 160, 0, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.6);
        }

        .key-concept-main::before {
            content: '💡 KEY CONCEPT — Today\'s Overview';
            position: absolute;
            top: -13px;
            left: 24px;
            font-size: 0.62em;
            font-weight: 800;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            color: #E65100;
            background: linear-gradient(90deg, #FFF8E1, #FFE082);
            padding: 2px 12px;
            border-radius: 100px;
            border: 1px solid rgba(255, 160, 0, 0.3);
        }

        /* Pencil decoration */
        .key-concept-main::after {
            content: '✏️';
            position: absolute;
            top: -12px;
            right: 24px;
            font-size: 1.2em;
            background: #FFF8E1;
            padding: 0 6px;
        }

        .key-concept-main p {
            color: #4A3728;
            line-height: 1.82;
            margin-bottom: 14px;
            font-size: 1.02em;
        }

        .key-concept-main p:last-child { margin-bottom: 0; }

        .key-concept-main strong {
            color: #2D1B0E;
            font-weight: 700;
            /* Yellow highlighter underline */
            background: linear-gradient(180deg, transparent 58%, rgba(255, 235, 59, 0.55) 58%);
            padding: 0 2px;
        }

        .key-concept-main code {
            font-family: "SF Mono", Monaco, "Consolas", monospace;
            background: rgba(255,255,255,0.6);
            border: 1px solid rgba(139, 115, 85, 0.2);
            padding: 1px 5px;
            border-radius: 4px;
            font-size: 0.88em;
        }

        .key-concept-main em {
            font-style: italic;
            color: #6D4C41;
        }

        /* ============================================================
         * CHAPTER SECTION
         * ============================================================ */
        .chapter-section {
            margin-bottom: 44px;
            position: relative;
        }

        /* Chapter Header */
        .chapter-head {
            display: flex;
            align-items: flex-end;
            gap: 20px;
            margin-bottom: 24px;
            padding-bottom: 16px;
            border-bottom: 3px solid;
            position: relative;
        }

        /* Pencil in top-right of chapter header */
        .chapter-head::after {
            content: '✏️';
            position: absolute;
            top: 0;
            right: 0;
            font-size: 0.8em;
            opacity: 0.35;
        }

        .chapter-num-block {
            flex-shrink: 0;
            text-align: center;
            min-width: 80px;
        }

        .chapter-label {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 0.62em;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.18em;
            color: #B8A084;
            margin-bottom: 2px;
        }

        .chapter-big-num {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 3.2em;
            font-weight: 700;
            line-height: 1;
        }

        .chapter-info {
            flex: 1;
            padding-bottom: 4px;
        }

        .chapter-emoji-row {
            font-size: 1.3em;
            margin-bottom: 4px;
        }

        .chapter-name {
            font-family: Georgia, "Noto Serif SC", "Source Han Serif SC", serif;
            font-size: 1.55em;
            font-weight: 700;
            color: #2D1B0E;
            line-height: 1.25;
            margin-bottom: 4px;
        }

        .chapter-item-count {
            font-size: 0.78em;
            color: #8B7355;
            font-style: italic;
        }

        .chapter-badge {
            align-self: center;
            font-size: 0.72em;
            font-weight: 800;
            color: #FFFFFF;
            padding: 6px 16px;
            border-radius: 100px;
            white-space: nowrap;
        }

        /* ============================================================
         * NOTE CARDS: Study notes with margin annotations
         * ============================================================ */
        .notes-container {
            background: #FFFFFF;
            border: 1px solid rgba(139, 115, 85, 0.18);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(45, 27, 14, 0.05);
        }

        .note-card {
            padding: 20px 24px;
            border-bottom: 1px solid rgba(139, 115, 85, 0.1);
            position: relative;
            page-break-inside: avoid;
            transition: background 0.2s;
        }

        .note-card:last-child { border-bottom: none; }

        .note-card:hover {
            background: rgba(255, 248, 240, 0.6);
        }

        /* Priority visual treatments */
        .note-card.must-read {
            /* Yellow highlighter wash */
            background: linear-gradient(90deg,
                rgba(255, 235, 59, 0.18) 0%,
                rgba(255, 235, 59, 0.08) 20%,
                rgba(255, 235, 59, 0.03) 50%,
                transparent 80%);
            border-left: 5px solid #FFD600;
        }

        .note-card.must-read:hover {
            background: linear-gradient(90deg,
                rgba(255, 235, 59, 0.25) 0%,
                rgba(255, 235, 59, 0.12) 20%,
                rgba(255, 248, 240, 0.8) 50%,
                rgba(255, 248, 240, 0.6) 80%);
        }

        .note-card.recommended {
            border-left: 4px solid #FF9800;
        }

        .note-card.fyi {
            border-left: 3px solid rgba(139, 115, 85, 0.2);
            opacity: 0.9;
        }

        /* Highlighter "MUST READ" label */
        .highlighter-tag {
            display: inline-block;
            background: linear-gradient(180deg,
                transparent 0%,
                transparent 35%,
                rgba(255, 235, 59, 0.6) 35%,
                rgba(255, 235, 59, 0.6) 85%,
                transparent 85%);
            font-size: 0.6em;
            font-weight: 800;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            color: #C62828;
            padding: 2px 6px;
            margin-bottom: 8px;
        }

        .note-top-row {
            display: flex;
            align-items: flex-start;
            gap: 10px;
            margin-bottom: 8px;
        }

        .note-priority {
            font-size: 1.05em;
            flex-shrink: 0;
            margin-top: 2px;
        }

        .note-headline {
            font-family: Georgia, "Noto Serif SC", "Source Han Serif SC", serif;
            font-size: 1.05em;
            font-weight: 700;
            line-height: 1.42;
            flex: 1;
        }

        .note-headline a { color: #2D1B0E; }
        .note-headline a:hover { color: #00695C; text-decoration: none; }

        .note-card.must-read .note-headline a {
            color: #C62828;
            font-weight: 800;
            /* Yellow highlight underline */
            text-decoration: underline;
            text-decoration-color: rgba(255, 235, 59, 0.7);
            text-underline-offset: 3px;
            text-decoration-thickness: 3px;
        }

        .note-meta-row {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 10px;
            flex-wrap: wrap;
        }

        .note-source {
            font-size: 0.73em;
            font-weight: 700;
            color: #00695C;
            background: rgba(0, 105, 92, 0.08);
            border: 1px solid rgba(0, 105, 92, 0.15);
            padding: 3px 10px;
            border-radius: 100px;
        }

        .note-tag {
            font-size: 0.68em;
            font-weight: 500;
            color: #8B7355;
            background: rgba(139, 115, 85, 0.08);
            border: 1px solid rgba(139, 115, 85, 0.15);
            padding: 2px 8px;
            border-radius: 100px;
        }

        /* Note detail + margin note layout */
        .note-body-grid {
            display: grid;
            grid-template-columns: 1fr 140px;
            gap: 16px;
        }

        .note-detail {
            color: #5C4033;
            font-size: 0.93em;
            line-height: 1.78;
            padding-left: calc(1.05em + 10px);
        }

        .note-detail strong {
            color: #2D1B0E;
            font-weight: 700;
            background: linear-gradient(180deg, transparent 58%, rgba(255, 235, 59, 0.35) 58%);
            padding: 0 1px;
        }

        .note-detail em {
            font-style: italic;
            color: #6D4C41;
        }

        .note-detail code {
            font-family: "SF Mono", Monaco, "Consolas", monospace;
            background: rgba(0, 0, 0, 0.04);
            border: 1px solid rgba(139, 115, 85, 0.15);
            padding: 1px 5px;
            border-radius: 4px;
            font-size: 0.88em;
        }

        /* Margin note (sidebar annotation) */
        .margin-note {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 0.7em;
            color: #00695C;
            font-style: italic;
            line-height: 1.52;
            padding: 10px 12px;
            background: rgba(0, 105, 92, 0.04);
            border-left: 2px solid rgba(0, 105, 92, 0.2);
            border-radius: 0 8px 8px 0;
            align-self: start;
        }

        .margin-note-icon {
            display: block;
            font-style: normal;
            font-size: 1.15em;
            margin-bottom: 4px;
        }

        @media (max-width: 768px) {
            .note-body-grid {
                grid-template-columns: 1fr;
            }
            .margin-note {
                border-left: none;
                border-top: 2px solid rgba(0, 105, 92, 0.15);
                border-radius: 0 0 8px 8px;
            }
        }

        /* ============================================================
         * KEY CONCEPT: In-chapter concept box
         * ============================================================ */
        .key-concept-box {
            background:
                linear-gradient(135deg,
                    #FFF3E0 0%,
                    #FFE0B2 50%,
                    #FFCC80 100%);
            border: 2px solid rgba(255, 152, 0, 0.25);
            border-radius: 10px;
            padding: 18px 22px;
            margin: 16px 0 8px;
            position: relative;
        }

        .key-concept-box::before {
            content: '💡 KEY CONCEPT';
            position: absolute;
            top: -11px;
            left: 16px;
            font-size: 0.58em;
            font-weight: 800;
            letter-spacing: 0.14em;
            color: #E65100;
            background: linear-gradient(90deg, #FFF3E0, #FFE0B2);
            padding: 2px 10px;
            border-radius: 100px;
            border: 1px solid rgba(255, 152, 0, 0.25);
        }

        .key-concept-box p {
            font-size: 0.9em;
            line-height: 1.68;
            color: #4A3728;
            font-weight: 500;
        }

        .key-concept-box strong { color: #E65100; }

        /* ============================================================
         * CHAPTER DIVIDER: Pencil-themed
         * ============================================================ */
        .pencil-divider {
            display: flex;
            align-items: center;
            gap: 12px;
            margin: 12px 0 44px;
            padding: 0;
        }

        .pencil-divider-line {
            flex: 1;
            height: 1px;
            background:
                repeating-linear-gradient(90deg,
                    rgba(139, 115, 85, 0.2) 0px,
                    rgba(139, 115, 85, 0.2) 6px,
                    transparent 6px,
                    transparent 10px);
        }

        .pencil-divider-icon {
            font-size: 0.9em;
            opacity: 0.5;
        }

        /* ============================================================
         * STUDY SUMMARY: End of document
         * ============================================================ */
        .study-summary {
            background: #FFFFFF;
            border: 2px solid rgba(139, 115, 85, 0.2);
            border-radius: 14px;
            padding: 28px;
            margin: 40px 0;
            text-align: center;
            page-break-inside: avoid;
            position: relative;
        }

        .study-summary::before {
            content: '📊';
            position: absolute;
            top: -15px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 1.4em;
            background: #FFF8F0;
            padding: 0 10px;
        }

        .study-summary-title {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 1.1em;
            font-weight: 700;
            color: #8B4513;
            margin-bottom: 16px;
        }

        .study-stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 12px;
        }

        .study-stat {
            padding: 12px;
            background: linear-gradient(135deg, #FFF8F0, #FFF3E0);
            border-radius: 10px;
            border: 1px solid rgba(139, 115, 85, 0.12);
        }

        .study-stat-num {
            font-family: Georgia, serif;
            font-size: 1.6em;
            font-weight: 700;
            line-height: 1;
            margin-bottom: 4px;
        }

        .study-stat-label {
            font-size: 0.68em;
            font-weight: 600;
            color: #8B7355;
            text-transform: uppercase;
            letter-spacing: 0.06em;
        }

        /* ============================================================
         * FOOTER: Academic closure
         * ============================================================ */
        .footer {
            text-align: center;
            padding: 36px 20px;
            margin-top: 40px;
            border-top: 3px solid #8B4513;
            position: relative;
        }

        .footer::before {
            content: '✏️';
            position: absolute;
            top: -14px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 1.2em;
            background: #FFF8F0;
            padding: 0 10px;
        }

        .footer-brand {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 1.2em;
            font-weight: 700;
            color: #8B4513;
            margin-bottom: 8px;
        }

        .footer-meta {
            font-size: 0.82em;
            color: #8B7355;
            line-height: 1.6;
            margin-bottom: 8px;
        }

        .footer-meta .hl {
            color: #5C4033;
            font-weight: 700;
        }

        .footer-tagline {
            font-family: Georgia, "Noto Serif SC", serif;
            font-size: 0.78em;
            color: #B8A084;
            font-style: italic;
            margin-bottom: 6px;
        }

        .footer-pencil-row {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            margin-top: 12px;
            font-size: 0.7em;
            color: #B8A084;
        }

        /* ============================================================
         * PRINT / PDF
         * ============================================================ */
        @media print {
            body {
                background: #FFF8F0 !important;
                font-size: 11pt;
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }

            .container {
                max-width: 100%;
                padding: 0 12px;
                padding-left: 12px;
            }

            .textbook-cover {
                margin: 0;
                border-radius: 0;
                box-shadow: none;
                page-break-after: avoid;
            }

            .textbook-cover::before,
            .textbook-cover::after { display: none; }

            .chapter-section { page-break-inside: avoid; }

            .note-card {
                break-inside: avoid;
                page-break-inside: avoid;
                transition: none;
            }

            .note-card:hover { background: transparent !important; }
            .note-card.must-read:hover { background: linear-gradient(90deg, rgba(255,235,59,0.18) 0%, transparent 50%) !important; }

            .notes-container { box-shadow: none; }

            .key-concept-main { box-shadow: none; }

            .toc { box-shadow: none; }

            a { color: #2D1B0E !important; text-decoration: none !important; }
        }

        /* ============================================================
         * RESPONSIVE
         * ============================================================ */
        @media (max-width: 768px) {
            .container {
                padding: 20px 16px 32px;
                padding-left: 16px;
            }

            /* Hide margin line on mobile */
            body {
                background:
                    repeating-linear-gradient(
                        0deg,
                        transparent,
                        transparent 31px,
                        rgba(180, 160, 130, 0.08) 31px,
                        rgba(180, 160, 130, 0.08) 32px
                    ),
                    linear-gradient(135deg, #FFF8F0 0%, #FFFDF7 50%, #FEF5E7 100%);
            }

            .textbook-cover {
                margin: 0 -16px 28px;
                padding: 36px 24px 32px;
                border-radius: 0;
            }

            .cover-title { font-size: 2em; }

            .chapter-head {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }

            .chapter-num-block {
                display: flex;
                align-items: baseline;
                gap: 8px;
                min-width: auto;
            }

            .chapter-big-num { font-size: 2em; }

            .chapter-badge { align-self: flex-start; }

            .note-card { padding: 16px; }
            .note-detail { padding-left: 0; }

            .toc-list {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 480px) {
            body { font-size: 14px; }

            .textbook-cover { padding: 28px 18px 24px; }
            .cover-title { font-size: 1.7em; }
            .cover-subtitle { font-size: 0.95em; }

            .chapter-name { font-size: 1.25em; }
            .chapter-big-num { font-size: 1.7em; }

            .note-headline { font-size: 0.95em; }

            .key-concept-main { padding: 24px 20px 20px; }

            .study-stats-grid { grid-template-columns: 1fr 1fr; }
        }
    </style>
</head>
<body>
    <div class="container">

        <!-- ====== HEADER: Textbook Cover ====== -->
        <header class="textbook-cover">
            <div class="cover-inner">
                <div class="cover-edition">Newsloom Knowledge Series · {{ date_str }} Edition</div>
                <h1 class="cover-title">📚 知识日报</h1>
                <div class="cover-subtitle">Daily Intelligence Compendium — Technology, Markets &amp; Innovation</div>
                <div class="cover-pencil-rule">
                    <div class="cover-pencil-rule-line"></div>
                    <span class="cover-pencil-icon">✏️</span>
                    <div class="cover-pencil-rule-line"></div>
                </div>
                {% set ns = namespace(must_count=0, rec_count=0, fyi_count=0) %}
                {% for section_key, items in briefs.items() %}
                {% if items is iterable and items is not string %}
                {% for item in items %}
                {% if item.priority == '🔴' %}{% set ns.must_count = ns.must_count + 1 %}
                {% elif item.priority == '🟡' %}{% set ns.rec_count = ns.rec_count + 1 %}
                {% else %}{% set ns.fyi_count = ns.fyi_count + 1 %}
                {% endif %}
                {% endfor %}
                {% endif %}
                {% endfor %}
                <div class="cover-meta-row">
                    <span class="cover-pill"><span class="cover-pill-emoji">📖</span>{{ total_items }} 篇文章</span>
                    <span class="cover-pill"><span class="cover-pill-emoji">⚡</span>{{ ns.must_count }} 必读</span>
                    <span class="cover-pill"><span class="cover-pill-emoji">📑</span>{{ section_order|length }} 章节</span>
                    <span class="cover-pill"><span class="cover-pill-emoji">🕐</span>{{ generated_time }}</span>
                </div>
            </div>
        </header>

        <!-- ====== TABLE OF CONTENTS ====== -->
        <nav class="toc">
            <div class="toc-title">目录 · Table of Contents</div>
            <ol class="toc-list">
                {% set ns_toc = namespace(num=1) %}
                {% for section_key in section_order %}
                {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
                {% set meta = section_configs.get(section_key, {}) %}
                <li class="toc-item">
                    <span class="toc-chapter-num">{{ ns_toc.num }}.</span>
                    {{ meta.emoji|default('📌') }}
                    <span class="toc-item-title">{{ meta.title|default(section_key) }}</span>
                    <span class="toc-dot-leader"></span>
                    <span class="toc-item-count">{{ briefs[section_key]|length }}</span>
                </li>
                {% set ns_toc.num = ns_toc.num + 1 %}
                {% endif %}
                {% endfor %}
            </ol>
        </nav>

        <!-- ====== EXECUTIVE SUMMARY: Key Concept ====== -->
        {% if executive_summary %}
        <div class="key-concept-main">
            {% for line in executive_summary.strip().split('\n') %}
            {% if line.strip() %}
            <p>{{ line.strip() | md_inline }}</p>
            {% endif %}
            {% endfor %}
        </div>
        {% endif %}

        <!-- ====== CHAPTERS ====== -->
        {% set ns_ch = namespace(num=1) %}
        {% for section_key in section_order %}
        {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
        {% set meta = section_configs.get(section_key, {}) %}
        {% set items = briefs[section_key] %}
        {% set sec_color = meta.color|default('#E65100') %}

        {% if ns_ch.num > 1 %}
        <div class="pencil-divider">
            <div class="pencil-divider-line"></div>
            <span class="pencil-divider-icon">✏️</span>
            <div class="pencil-divider-line"></div>
        </div>
        {% endif %}

        <section class="chapter-section">
            <!-- Chapter Header -->
            <div class="chapter-head" style="border-bottom-color: {{ sec_color }};">
                <div class="chapter-num-block">
                    <div class="chapter-label">Chapter</div>
                    <div class="chapter-big-num" style="color: {{ sec_color }};">{{ ns_ch.num }}</div>
                </div>
                <div class="chapter-info">
                    <div class="chapter-emoji-row">{{ meta.emoji|default('📌') }}</div>
                    <h2 class="chapter-name">{{ meta.title|default(section_key) }}</h2>
                    <div class="chapter-item-count">{{ items|length }} articles in this chapter</div>
                </div>
                <span class="chapter-badge" style="background: {{ sec_color }};">{{ items|length }}</span>
            </div>

            <!-- Note Cards -->
            <div class="notes-container">
                {% set ns_item = namespace(idx=0) %}
                {% for item in items %}
                {% set priority = item.priority|default('🟢') %}
                {% set p_class = 'must-read' if priority == '🔴' else ('recommended' if priority == '🟡' else 'fyi') %}
                {% set ns_item.idx = ns_item.idx + 1 %}

                <div class="note-card {{ p_class }}">
                    {% if priority == '🔴' %}
                    <div><span class="highlighter-tag">📌 Must Read</span></div>
                    {% endif %}

                    <div class="note-top-row">
                        <span class="note-priority">{{ priority }}</span>
                        <h3 class="note-headline">
                            <a href="{{ item.url|default('#') }}" target="_blank">{{ item.headline|default('无标题') }}</a>
                        </h3>
                    </div>

                    {% if item.source or item.tags %}
                    <div class="note-meta-row">
                        {% if item.source %}
                        <span class="note-source">{{ item.source }}</span>
                        {% endif %}
                        {% if item.tags %}
                        {% for tag in item.tags %}
                        <span class="note-tag">{{ tag }}</span>
                        {% endfor %}
                        {% endif %}
                    </div>
                    {% endif %}

                    {% if item.detail %}
                    {% if priority == '🔴' and item.source %}
                    <!-- Must-read items get a margin note -->
                    <div class="note-body-grid">
                        <div class="note-detail">{{ item.detail | md_inline }}</div>
                        <div class="margin-note">
                            <span class="margin-note-icon">📝</span>
                            来源: {{ item.source }} · 优先级最高，建议精读全文
                        </div>
                    </div>
                    {% else %}
                    <div class="note-detail">{{ item.detail | md_inline }}</div>
                    {% endif %}
                    {% endif %}
                </div>
                {% endfor %}
            </div>

            <!-- Key Concept box after first chapter -->
            {% if ns_ch.num == 1 and ns.must_count > 0 %}
            <div class="key-concept-box">
                <p>本期日报共收录 <strong>{{ total_items }}</strong> 篇情报，横跨 <strong>{{ section_order|length }}</strong> 个知识领域，其中 <strong>{{ ns.must_count }}</strong> 篇被标记为高优先级必读内容。</p>
            </div>
            {% endif %}
        </section>

        {% set ns_ch.num = ns_ch.num + 1 %}
        {% endif %}
        {% endfor %}

        <!-- ====== STUDY SUMMARY ====== -->
        <div class="study-summary">
            <div class="study-summary-title">📝 Study Summary · 学习小结</div>
            <div class="study-stats-grid">
                <div class="study-stat">
                    <div class="study-stat-num" style="color: #8B4513;">{{ total_items }}</div>
                    <div class="study-stat-label">总文章</div>
                </div>
                <div class="study-stat">
                    <div class="study-stat-num" style="color: #C62828;">{{ ns.must_count }}</div>
                    <div class="study-stat-label">必读</div>
                </div>
                <div class="study-stat">
                    <div class="study-stat-num" style="color: #E65100;">{{ ns.rec_count }}</div>
                    <div class="study-stat-label">推荐</div>
                </div>
                <div class="study-stat">
                    <div class="study-stat-num" style="color: #00695C;">{{ ns_ch.num - 1 }}</div>
                    <div class="study-stat-label">章节</div>
                </div>
            </div>
        </div>

        <!-- ====== FOOTER ====== -->
        <footer class="footer">
            <div class="footer-brand">📚 Newsloom 知识日报</div>
            <div class="footer-meta">
                AI 精选整理 · <span class="hl">{{ date_str }}</span> ·
                共分析 <span class="hl">{{ total_items }}</span> 个来源 ·
                生成于 <span class="hl">{{ generated_time }}</span>
            </div>
            <div class="footer-tagline">学无止境，日进一步 — Learning never stops</div>
            <div class="footer-pencil-row">
                ✏️ · ✏️ · ✏️
            </div>
        </footer>

    </div>
</body>
</html>