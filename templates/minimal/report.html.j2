<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Report - {{ date_str }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Georgia, serif;
            line-height: 1.8;
            color: #333;
            background: #fff;
            padding: 40px 20px;
            max-width: 800px;
            margin: 0 auto;
        }

        header {
            border-bottom: 3px solid #000;
            padding-bottom: 20px;
            margin-bottom: 40px;
        }

        h1 {
            font-size: 2.5em;
            font-weight: normal;
            margin-bottom: 10px;
        }

        .meta {
            color: #666;
            font-size: 0.9em;
            font-family: -apple-system, sans-serif;
        }

        .section {
            margin-bottom: 60px;
        }

        .section-title {
            font-size: 1.8em;
            font-weight: normal;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 1px solid #ddd;
        }

        .item {
            margin-bottom: 40px;
        }

        .item-title {
            font-size: 1.3em;
            margin-bottom: 10px;
        }

        .item-title a {
            color: #000;
            text-decoration: none;
        }

        .item-title a:hover {
            text-decoration: underline;
        }

        .item-meta {
            font-size: 0.85em;
            color: #999;
            margin-bottom: 10px;
            font-family: -apple-system, sans-serif;
        }

        .item-text {
            color: #444;
        }

        footer {
            margin-top: 80px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            text-align: center;
            color: #999;
            font-size: 0.9em;
        }

        @media print {
            body {
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Daily Report</h1>
        <p class="meta">{{ date_str }} • {{ total_items }} items</p>
    </header>

    {% for section_key in section_order %}
    {% if section_key in briefs and briefs[section_key]|length > 0 %}
    {% set section_meta = section_configs[section_key] %}
    {% set items = briefs[section_key] %}
    <section class="section">
        <h2 class="section-title">{{ section_meta.emoji }} {{ section_meta.title }}</h2>

        {% for item in items %}
        <article class="item">
            <h3 class="item-title">
                <a href="{{ item.url }}" target="_blank">{{ item.title }}</a>
            </h3>
            {% if item.source %}
            <div class="item-meta">{{ item.source }}</div>
            {% endif %}
            <div class="item-text">{{ item.text }}</div>
        </article>
        {% endfor %}
    </section>
    {% endif %}
    {% endfor %}

    <footer>
        <p>Generated by Newsloom</p>
    </footer>
</body>
</html>
