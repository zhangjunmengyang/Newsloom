<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Newsloom Daily Brief · {{ date_str }}</title>
    <style>
        /* ============================================================
         * Newsloom Newsletter Template
         * Linear reading · High info density · PDF-friendly
         * Inspired by: Morning Brew, The Information, Stratechery
         * ============================================================ */

        @page {
            size: A4;
            margin: 2cm 1.5cm;
            @bottom-center {
                content: "📰 Newsloom · {{ date_str }} · Page " counter(page);
                font-size: 8pt;
                color: #94a3b8;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", sans-serif;
            background: #fafafa;
            color: #1a1a2e;
            font-size: 15px;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        .container {
            max-width: 700px;
            margin: 0 auto;
            padding: 32px 20px;
            background: #ffffff;
        }

        /* Header - One-liner */
        .header {
            padding: 20px 0 24px;
            border-bottom: 2px solid #e2e8f0;
            margin-bottom: 28px;
        }

        .header-title {
            font-size: 1.3em;
            font-weight: 700;
            color: #1a1a2e;
            margin-bottom: 6px;
        }

        .header-meta {
            font-size: 0.85em;
            color: #64748b;
        }

        .header-meta span {
            margin-right: 12px;
        }

        /* Executive Summary - High-visibility block */
        .exec-summary {
            background: #fffbeb;
            border-left: 4px solid #f59e0b;
            border-radius: 6px;
            padding: 20px 24px;
            margin-bottom: 32px;
            page-break-inside: avoid;
        }

        .exec-summary h2 {
            font-size: 1.05em;
            font-weight: 700;
            color: #d97706;
            margin-bottom: 12px;
            text-transform: uppercase;
            letter-spacing: 0.02em;
        }

        .exec-summary p {
            color: #451a03;
            font-size: 0.95em;
            margin-bottom: 6px;
            line-height: 1.7;
        }

        .exec-summary p:last-child {
            margin-bottom: 0;
        }

        /* Section Header - Compact */
        .section {
            margin-bottom: 36px;
            page-break-inside: avoid;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 8px;
            padding-bottom: 10px;
            margin-bottom: 16px;
            border-bottom: 1px solid #e2e8f0;
        }

        .section-emoji {
            font-size: 1.3em;
        }

        .section-title {
            font-size: 1.25em;
            font-weight: 700;
            color: #1a1a2e;
        }

        .section-count {
            margin-left: auto;
            font-size: 0.75em;
            color: #94a3b8;
            background: #f1f5f9;
            padding: 2px 8px;
            border-radius: 4px;
            font-weight: 600;
        }

        /* Brief Card - Compact & high density */
        .brief {
            display: flex;
            margin-bottom: 16px;
            page-break-inside: avoid;
        }

        .brief-priority {
            width: 3px;
            flex-shrink: 0;
            border-radius: 2px 0 0 2px;
            margin-right: 14px;
        }

        .brief-priority.must-read {
            background: #ef4444;
        }

        .brief-priority.recommended {
            background: #f59e0b;
        }

        .brief-priority.fyi {
            background: #22c55e;
        }

        .brief-body {
            flex: 1;
        }

        .brief-headline {
            font-size: 1em;
            font-weight: 600;
            margin-bottom: 4px;
            line-height: 1.4;
        }

        .brief-headline a {
            color: #1a1a2e;
            text-decoration: none;
            transition: color 0.15s;
        }

        .brief-headline a:hover {
            color: #6366f1;
            text-decoration: underline;
        }

        .brief-meta {
            display: flex;
            align-items: center;
            gap: 6px;
            margin-bottom: 6px;
            flex-wrap: wrap;
        }

        .source-badge {
            font-size: 0.78em;
            color: #64748b;
            background: #f1f5f9;
            padding: 2px 8px;
            border-radius: 3px;
            font-weight: 500;
        }

        .tag {
            font-size: 0.72em;
            color: #6366f1;
            background: #eef2ff;
            padding: 2px 6px;
            border-radius: 3px;
            font-weight: 500;
        }

        .brief-detail {
            color: #64748b;
            font-size: 0.92em;
            line-height: 1.6;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 24px 0;
            color: #94a3b8;
            border-top: 1px solid #e2e8f0;
            margin-top: 32px;
            font-size: 0.8em;
        }

        /* Print optimizations */
        @media print {
            body {
                background: #ffffff;
            }

            .container {
                max-width: 100%;
                padding: 0;
            }

            .brief {
                margin-bottom: 12px;
            }

            .section {
                margin-bottom: 28px;
            }

            .exec-summary {
                page-break-after: avoid;
            }

            a {
                color: #1a1a2e;
                text-decoration: none;
            }

            a[href]:after {
                content: " (" attr(href) ")";
                font-size: 0.7em;
                color: #94a3b8;
            }
        }

        /* Mobile */
        @media (max-width: 640px) {
            .container {
                padding: 20px 16px;
            }

            .header-title {
                font-size: 1.1em;
            }

            .section-title {
                font-size: 1.1em;
            }

            .brief-priority {
                margin-right: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header: One-liner -->
        <header class="header">
            <div class="header-title">📰 Newsloom Daily Brief · {{ date_str }}</div>
            <div class="header-meta">
                <span>{{ generated_time }}</span>
                <span>·</span>
                <span>{{ total_items }} items</span>
                {% set must_read_total = [] %}
                {% for section_key, items in briefs.items() %}
                {% if items is iterable and items is not string %}
                {% for item in items %}
                {% if item.priority == '🔴' %}
                {% if must_read_total.append(1) %}{% endif %}
                {% endif %}
                {% endfor %}
                {% endif %}
                {% endfor %}
                {% if must_read_total|length > 0 %}
                <span>·</span>
                <span style="color: #ef4444; font-weight: 600;">{{ must_read_total|length }} must-read</span>
                {% endif %}
            </div>
        </header>

        <!-- Executive Summary: Yellow highlight block -->
        {% if executive_summary %}
        <div class="exec-summary">
            <h2>⚡ Executive Summary</h2>
            {% for line in executive_summary.strip().split('\n') %}
            {% if line.strip() %}
            <p>{{ line }}</p>
            {% endif %}
            {% endfor %}
        </div>
        {% endif %}

        <!-- Sections: Linear top-to-bottom -->
        {% for section_key in section_order %}
        {% if section_key in briefs and briefs[section_key]|length > 0 %}
        {% set meta = section_configs[section_key] %}
        {% set items = briefs[section_key] %}
        <section class="section">
            <div class="section-header">
                <span class="section-emoji">{{ meta.emoji }}</span>
                <h2 class="section-title">{{ meta.title }}</h2>
                <span class="section-count">{{ items|length }}</span>
            </div>

            {% for item in items %}
            {% set priority = item.priority|default('🟢') %}
            {% set priority_class = 'must-read' if priority == '🔴' else ('recommended' if priority == '🟡' else 'fyi') %}
            <div class="brief">
                <div class="brief-priority {{ priority_class }}"></div>
                <div class="brief-body">
                    <div class="brief-headline">
                        <a href="{{ item.url }}" target="_blank">{{ item.headline|default(item.title|default('No title')) }}</a>
                    </div>
                    <div class="brief-meta">
                        {% if item.source %}
                        <span class="source-badge">{{ item.source }}</span>
                        {% endif %}
                        {% if item.tags %}
                        {% for tag in item.tags %}
                        <span class="tag">{{ tag }}</span>
                        {% endfor %}
                        {% endif %}
                    </div>
                    {% if item.detail %}
                    <div class="brief-detail">{{ item.detail|default(item.text|default('')) }}</div>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </section>
        {% endif %}
        {% endfor %}

        <!-- Footer -->
        <footer>
            Generated by Newsloom · {{ date_str }}
        </footer>
    </div>
</body>
</html>
