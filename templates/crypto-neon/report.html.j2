<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Newsloom Crypto Terminal · {{ date_str }}</title>
    <style>
        /* ============================================================
         * CRYPTO NEON v2 — Dark Exchange Terminal
         * Deep black (#0D1117) + BTC Orange (#F7931A) +
         * ETH Purple (#627EEA) + Neon Green (#00FF88)
         *
         * Visual Narrative:
         *   - Hexagonal honeycomb grid background (CSS gradients)
         *   - Ticker-board style header with big stat numbers
         *   - Dark terminal window cards with neon borders
         *   - Price up/down priority styling (green rise, red fall)
         *   - Blinking online indicator lights
         *   - Monospace on-chain hash style footer
         *
         * Inspired by: Binance, CoinGecko, Bloomberg Terminal,
         *              TradingView dark mode, DeFi dashboards
         * ============================================================ */

        :root {
            /* === Core Palette === */
            --bg-void:        #080b10;
            --bg-primary:     #0D1117;
            --bg-secondary:   #161B22;
            --bg-card:        #1C2128;
            --bg-surface:     #21262D;
            --bg-hover:       #292E36;
            --bg-elevated:    #30363D;

            /* === Neon Colors === */
            --btc-orange:      #F7931A;
            --btc-orange-dim:  rgba(247, 147, 26, 0.15);
            --btc-orange-glow: rgba(247, 147, 26, 0.30);
            --eth-purple:      #627EEA;
            --eth-purple-dim:  rgba(98, 126, 234, 0.12);
            --eth-purple-glow: rgba(98, 126, 234, 0.25);
            --neon-green:      #00FF88;
            --neon-green-dim:  rgba(0, 255, 136, 0.10);
            --neon-green-glow: rgba(0, 255, 136, 0.20);
            --neon-cyan:       #00D4FF;
            --neon-cyan-dim:   rgba(0, 212, 255, 0.10);
            --neon-pink:       #FF006E;
            --neon-pink-dim:   rgba(255, 0, 110, 0.10);

            /* === Priority (Crypto: green=rise, red=fall) === */
            --bull-green:          #00FF88;
            --bull-green-bg:       rgba(0, 255, 136, 0.08);
            --bull-green-border:   rgba(0, 255, 136, 0.30);
            --bull-green-glow:     rgba(0, 255, 136, 0.20);
            --bear-red:            #FF4444;
            --bear-red-bg:         rgba(255, 68, 68, 0.08);
            --bear-red-border:     rgba(255, 68, 68, 0.30);
            --bear-red-glow:       rgba(255, 68, 68, 0.20);
            --neutral-amber:       #FFB800;
            --neutral-amber-bg:    rgba(255, 184, 0, 0.06);
            --neutral-amber-border:rgba(255, 184, 0, 0.22);

            /* === Text === */
            --text-primary:    #E6EDF3;
            --text-secondary:  #B0BAC6;
            --text-muted:      #7D8590;
            --text-dim:        #484F58;
            --text-bright:     #FFFFFF;

            /* === Borders === */
            --border:          rgba(48, 54, 61, 0.80);
            --border-medium:   rgba(48, 54, 61, 1.0);
            --border-neon:     rgba(0, 255, 136, 0.15);

            /* === Typography === */
            --font-mono: "SF Mono", "JetBrains Mono", "Fira Code", "Cascadia Code", "Consolas", monospace;
            --font-sans: -apple-system, BlinkMacSystemFont, "Inter", "PingFang SC", "Noto Sans SC", "Microsoft YaHei", sans-serif;
            --font-cjk: "PingFang SC", "Noto Sans SC", "Hiragino Sans GB", "Microsoft YaHei", "Source Han Sans SC", sans-serif;
            --font-display: "SF Pro Display", -apple-system, "Inter", var(--font-cjk);

            /* === Spacing (8pt) === */
            --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;
            --sp-4: 16px; --sp-5: 20px; --sp-6: 24px;
            --sp-8: 32px; --sp-10: 40px; --sp-12: 48px;
            --sp-16: 64px; --sp-20: 80px;

            /* === Radius === */
            --radius-sm: 4px;
            --radius-md: 6px;
            --radius-lg: 10px;
            --radius-xl: 14px;

            /* === Shadows === */
            --shadow-card:     0 1px 3px rgba(0,0,0,0.50), 0 4px 12px rgba(0,0,0,0.30);
            --shadow-elevated: 0 4px 16px rgba(0,0,0,0.60), 0 8px 32px rgba(0,0,0,0.40);
            --shadow-neon-green: 0 0 15px var(--neon-green-glow), 0 0 30px rgba(0, 255, 136, 0.08);
            --shadow-neon-orange: 0 0 15px var(--btc-orange-glow), 0 0 30px rgba(247, 147, 26, 0.08);
            --shadow-neon-purple: 0 0 15px var(--eth-purple-glow), 0 0 30px rgba(98, 126, 234, 0.08);
        }

        /* --- Page (PDF) --- */
        @page {
            size: A4;
            margin: 1.5cm 1.8cm;
            @bottom-center {
                content: "Newsloom Crypto Terminal · " "{{ date_str }}" " · Page " counter(page);
                font-size: 7pt;
                color: var(--text-muted);
                font-family: var(--font-mono);
                letter-spacing: 0.05em;
            }
        }

        /* --- Reset --- */
        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: var(--font-sans);
            color: var(--text-primary);
            font-size: 15px;
            line-height: 1.7;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            min-height: 100vh;

            /* === HEXAGONAL HONEYCOMB GRID BACKGROUND ===
             * Three sets of repeating-linear-gradient at 0°, 60°, 120°
             * to create a hex-mesh overlay on the deep black base
             */
            background:
                /* Hex grid: 0° lines */
                repeating-linear-gradient(
                    0deg,
                    transparent,
                    transparent 28px,
                    rgba(0, 255, 136, 0.018) 28px,
                    rgba(0, 255, 136, 0.018) 29px
                ),
                /* Hex grid: 60° lines */
                repeating-linear-gradient(
                    60deg,
                    transparent,
                    transparent 28px,
                    rgba(98, 126, 234, 0.015) 28px,
                    rgba(98, 126, 234, 0.015) 29px
                ),
                /* Hex grid: 120° lines */
                repeating-linear-gradient(
                    120deg,
                    transparent,
                    transparent 28px,
                    rgba(247, 147, 26, 0.012) 28px,
                    rgba(247, 147, 26, 0.012) 29px
                ),
                /* Top BTC orange glow */
                radial-gradient(ellipse at 70% 0%, rgba(247, 147, 26, 0.06) 0%, transparent 40%),
                /* Left ETH purple glow */
                radial-gradient(ellipse at 10% 30%, rgba(98, 126, 234, 0.04) 0%, transparent 35%),
                /* Bottom green glow */
                radial-gradient(ellipse at 50% 100%, rgba(0, 255, 136, 0.03) 0%, transparent 40%),
                /* Base gradient */
                linear-gradient(180deg, #0D1117 0%, #0B0F14 40%, #0D1117 70%, #080B10 100%);
            background-attachment: fixed;
        }

        a {
            color: var(--neon-green);
            text-decoration: none;
            transition: all 0.2s ease;
        }
        a:hover {
            color: var(--neon-cyan);
            text-shadow: 0 0 8px var(--neon-green-glow);
        }

        code {
            font-family: var(--font-mono);
            background: rgba(0, 255, 136, 0.06);
            color: var(--neon-green);
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 0.88em;
            border: 1px solid rgba(0, 255, 136, 0.12);
        }

        strong {
            color: var(--text-bright);
            font-weight: 600;
        }

        /* --- Container --- */
        .container {
            max-width: 820px;
            margin: 0 auto;
            padding: var(--sp-10) var(--sp-6);
        }

        /* ==========================================================
         * BLINKING ONLINE INDICATOR (used in multiple places)
         * ========================================================== */
        @keyframes blink-online {
            0%, 100% { opacity: 1; box-shadow: 0 0 6px currentColor; }
            50% { opacity: 0.3; box-shadow: 0 0 2px currentColor; }
        }

        @keyframes pulse-glow {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        @keyframes scan-line {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(100vh); }
        }

        .online-dot {
            display: inline-block;
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--neon-green);
            animation: blink-online 2s ease-in-out infinite;
            color: var(--neon-green);
        }

        .online-dot.orange {
            background: var(--btc-orange);
            color: var(--btc-orange);
        }

        .online-dot.purple {
            background: var(--eth-purple);
            color: var(--eth-purple);
        }

        /* ==========================================================
         * HEADER — Exchange Ticker Board
         * ========================================================== */
        .header {
            position: relative;
            padding: var(--sp-12) var(--sp-8);
            margin-bottom: var(--sp-10);
            background:
                /* Hex overlay in header */
                repeating-linear-gradient(0deg, transparent, transparent 20px, rgba(0,255,136,0.025) 20px, rgba(0,255,136,0.025) 21px),
                repeating-linear-gradient(60deg, transparent, transparent 20px, rgba(98,126,234,0.020) 20px, rgba(98,126,234,0.020) 21px),
                repeating-linear-gradient(120deg, transparent, transparent 20px, rgba(247,147,26,0.015) 20px, rgba(247,147,26,0.015) 21px),
                linear-gradient(180deg, #0D1117 0%, #161B22 100%);
            border: 1px solid var(--border-medium);
            border-radius: var(--radius-xl);
            overflow: hidden;
        }

        /* Top neon green line */
        .header::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg,
                transparent 5%,
                var(--neon-green) 25%,
                var(--btc-orange) 50%,
                var(--eth-purple) 75%,
                transparent 95%);
            opacity: 0.8;
        }

        /* Scanline effect */
        .header::after {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 4px;
            background: linear-gradient(180deg, rgba(0,255,136,0.08), transparent);
            animation: scan-line 6s linear infinite;
            pointer-events: none;
        }

        .header-top-bar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-family: var(--font-mono);
            font-size: 0.65em;
            color: var(--text-muted);
            margin-bottom: var(--sp-6);
            padding-bottom: var(--sp-3);
            border-bottom: 1px solid var(--border);
        }

        .header-top-bar .live-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            color: var(--neon-green);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.15em;
        }

        .header-top-bar .session-id {
            color: var(--text-dim);
            letter-spacing: 0.05em;
        }

        .header-brand {
            font-family: var(--font-mono);
            font-size: 0.72em;
            font-weight: 700;
            letter-spacing: 0.30em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: var(--sp-3);
        }

        .header-brand .accent-green { color: var(--neon-green); }
        .header-brand .accent-orange { color: var(--btc-orange); }

        .header h1 {
            font-family: var(--font-display);
            font-size: 2.8em;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: var(--sp-2);
            background: linear-gradient(135deg,
                var(--text-bright) 0%,
                var(--text-primary) 40%,
                var(--neon-green) 70%,
                var(--btc-orange) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .header .subtitle {
            font-family: var(--font-mono);
            font-size: 0.82em;
            color: var(--text-muted);
            letter-spacing: 0.06em;
            margin-bottom: var(--sp-8);
        }

        /* Ticker stats row — like an exchange dashboard */
        .ticker-stats {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--sp-3);
        }

        .ticker-stat {
            text-align: center;
            padding: var(--sp-4) var(--sp-3);
            background: rgba(0, 0, 0, 0.30);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            transition: border-color 0.3s;
        }

        .ticker-stat:hover {
            border-color: var(--neon-green);
            box-shadow: var(--shadow-neon-green);
        }

        .ticker-stat .stat-label {
            font-family: var(--font-mono);
            font-size: 0.62em;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            color: var(--text-muted);
            margin-bottom: var(--sp-1);
        }

        .ticker-stat .stat-value {
            font-family: var(--font-mono);
            font-size: 1.8em;
            font-weight: 800;
            line-height: 1.2;
        }

        .ticker-stat .stat-value.green { color: var(--neon-green); text-shadow: 0 0 12px var(--neon-green-glow); }
        .ticker-stat .stat-value.orange { color: var(--btc-orange); text-shadow: 0 0 12px var(--btc-orange-glow); }
        .ticker-stat .stat-value.purple { color: var(--eth-purple); text-shadow: 0 0 12px var(--eth-purple-glow); }
        .ticker-stat .stat-value.white { color: var(--text-bright); }

        .ticker-stat .stat-sub {
            font-family: var(--font-mono);
            font-size: 0.58em;
            color: var(--text-dim);
            margin-top: 2px;
        }

        /* ==========================================================
         * EXECUTIVE SUMMARY — Terminal Output Block
         * ========================================================== */
        .exec-summary {
            position: relative;
            margin-bottom: var(--sp-10);
            page-break-inside: avoid;
            background: var(--bg-secondary);
            border: 1px solid var(--border-medium);
            border-radius: var(--radius-lg);
            overflow: hidden;
        }

        /* Terminal title bar */
        .exec-titlebar {
            display: flex;
            align-items: center;
            gap: var(--sp-3);
            padding: var(--sp-2) var(--sp-4);
            background: var(--bg-surface);
            border-bottom: 1px solid var(--border);
            font-family: var(--font-mono);
            font-size: 0.70em;
        }

        .exec-titlebar .dots {
            display: flex;
            gap: 5px;
        }
        .exec-titlebar .dot {
            width: 10px; height: 10px;
            border-radius: 50%;
        }
        .exec-titlebar .dot.red { background: #FF5F56; }
        .exec-titlebar .dot.yellow { background: #FFBD2E; }
        .exec-titlebar .dot.green { background: #27C93F; }

        .exec-titlebar .title {
            flex: 1;
            text-align: center;
            color: var(--text-muted);
            letter-spacing: 0.08em;
        }

        .exec-titlebar .online-indicator {
            display: flex;
            align-items: center;
            gap: 5px;
            color: var(--neon-green);
            font-weight: 600;
        }

        .exec-body {
            padding: var(--sp-6) var(--sp-6);
        }

        .exec-prompt {
            font-family: var(--font-mono);
            font-size: 0.70em;
            color: var(--neon-green);
            margin-bottom: var(--sp-4);
            letter-spacing: 0.04em;
        }

        .exec-prompt .cursor {
            display: inline-block;
            width: 8px;
            height: 14px;
            background: var(--neon-green);
            vertical-align: middle;
            margin-left: 4px;
            animation: blink-online 1s step-end infinite;
        }

        .exec-body p {
            font-family: var(--font-sans);
            color: var(--text-secondary);
            font-size: 0.95em;
            line-height: 1.82;
            margin-bottom: var(--sp-4);
            padding-left: var(--sp-5);
            border-left: 2px solid var(--border);
        }

        .exec-body p:first-of-type {
            color: var(--text-primary);
            border-left-color: var(--neon-green);
        }

        .exec-body p:nth-of-type(2) {
            border-left-color: var(--btc-orange);
        }

        .exec-body p:nth-of-type(3) {
            border-left-color: var(--eth-purple);
        }

        .exec-body p:last-of-type { margin-bottom: 0; }

        .exec-body p strong {
            color: var(--neon-green);
            font-weight: 700;
        }

        .exec-body p code {
            font-family: var(--font-mono);
            background: rgba(0, 255, 136, 0.06);
            color: var(--neon-green);
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 0.88em;
            border: 1px solid rgba(0, 255, 136, 0.12);
        }

        /* ==========================================================
         * SECTION SEPARATOR — Circuit-line divider
         * ========================================================== */
        .circuit-separator {
            display: flex;
            align-items: center;
            gap: var(--sp-3);
            margin: var(--sp-8) 0;
            padding: var(--sp-1) 0;
        }

        .circuit-separator::before,
        .circuit-separator::after {
            content: '';
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-medium), transparent);
        }

        .circuit-separator .node-dot {
            width: 8px; height: 8px;
            border-radius: 50%;
            background: var(--neon-green);
            box-shadow: 0 0 8px var(--neon-green-glow);
            animation: blink-online 3s ease-in-out infinite;
        }

        .circuit-separator .line-segment {
            width: 24px;
            height: 1px;
            background: var(--border-medium);
        }

        /* ==========================================================
         * SECTION — Terminal Window Groups
         * ========================================================== */
        .section {
            margin-bottom: var(--sp-10);
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: var(--sp-3);
            padding: var(--sp-3) var(--sp-5);
            margin-bottom: var(--sp-5);
            position: relative;
            background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-secondary) 100%);
            border: 1px solid var(--border-medium);
            border-radius: var(--radius-md);
            overflow: hidden;
        }

        /* Left color accent bar — uses section color */
        .section-header .color-bar {
            position: absolute;
            left: 0; top: 0; bottom: 0;
            width: 3px;
            border-radius: var(--radius-md) 0 0 var(--radius-md);
        }

        .section-emoji {
            font-size: 1.15em;
            width: 36px; height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(0, 0, 0, 0.25);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
        }

        .section-header h2 {
            font-family: var(--font-sans);
            font-size: 1.15em;
            font-weight: 700;
            color: var(--text-primary);
            letter-spacing: 0.02em;
        }

        .section-header .online-badge {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            margin-left: var(--sp-1);
        }

        .section-count {
            margin-left: auto;
            font-family: var(--font-mono);
            font-size: 0.68em;
            font-weight: 700;
            padding: 3px 12px;
            border-radius: var(--radius-sm);
            letter-spacing: 0.06em;
            border: 1px solid var(--border);
            background: rgba(0, 0, 0, 0.20);
        }

        /* ==========================================================
         * BRIEF CARDS — Dark Terminal Windows with Neon Borders
         * ========================================================== */
        .briefs-grid {
            display: flex;
            flex-direction: column;
            gap: var(--sp-3);
        }

        .brief {
            position: relative;
            display: flex;
            gap: var(--sp-4);
            padding: var(--sp-5) var(--sp-5);
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: var(--radius-lg);
            transition: all 0.25s ease;
            page-break-inside: avoid;
            box-shadow: var(--shadow-card);
        }

        .brief:hover {
            background: var(--bg-hover);
            transform: translateY(-1px);
        }

        /* Priority left neon stripe */
        .brief-neon-stripe {
            position: absolute;
            left: 0; top: 0; bottom: 0;
            width: 3px;
            border-radius: var(--radius-lg) 0 0 var(--radius-lg);
        }

        /* === MUST-READ (🔴 = BEARISH / CRITICAL ALERT) === */
        .brief.alert-critical {
            border-color: var(--bear-red-border);
            background: linear-gradient(135deg,
                var(--bear-red-bg) 0%,
                var(--bg-card) 60%);
            box-shadow: 0 0 20px var(--bear-red-glow), var(--shadow-card);
        }

        .brief.alert-critical .brief-neon-stripe {
            background: linear-gradient(180deg, var(--bear-red), #FF6666);
            box-shadow: 0 0 8px var(--bear-red-glow);
        }

        .brief.alert-critical:hover {
            box-shadow: 0 0 30px var(--bear-red-glow), var(--shadow-elevated);
        }

        .alert-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            font-family: var(--font-mono);
            font-size: 0.55em;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.10em;
            color: var(--bear-red);
            background: var(--bear-red-bg);
            border: 1px solid var(--bear-red-border);
            padding: 2px 10px;
            border-radius: var(--radius-sm);
            margin-left: var(--sp-2);
            vertical-align: middle;
        }

        .alert-badge .blink-dot {
            width: 5px; height: 5px;
            background: var(--bear-red);
            border-radius: 50%;
            animation: blink-online 1.2s ease-in-out infinite;
            color: var(--bear-red);
        }

        /* === RECOMMENDED (🟡 = AMBER / WATCH) === */
        .brief.alert-watch {
            border-color: var(--neutral-amber-border);
        }

        .brief.alert-watch .brief-neon-stripe {
            background: linear-gradient(180deg, var(--neutral-amber), #FFD060);
            box-shadow: 0 0 6px rgba(255, 184, 0, 0.15);
        }

        /* === FYI (🟢 = GREEN / NORMAL) === */
        .brief.alert-normal {
            border-color: var(--border);
        }

        .brief.alert-normal .brief-neon-stripe {
            background: linear-gradient(180deg, var(--neon-green), #00CC6A);
            opacity: 0.5;
        }

        .brief-content {
            flex: 1;
            min-width: 0;
            padding-left: var(--sp-2);
        }

        .brief-headline {
            font-family: var(--font-sans);
            font-size: 1.02em;
            font-weight: 600;
            line-height: 1.4;
            margin-bottom: var(--sp-2);
        }

        .brief-headline a {
            color: var(--text-primary);
            transition: color 0.2s;
        }

        .brief-headline a:hover {
            color: var(--neon-green);
            text-shadow: 0 0 8px var(--neon-green-glow);
        }

        .brief.alert-critical .brief-headline a {
            color: var(--text-bright);
            font-weight: 700;
        }

        .brief-detail {
            color: var(--text-secondary);
            font-size: 0.90em;
            line-height: 1.75;
            margin-bottom: var(--sp-3);
        }

        .brief-detail strong {
            color: var(--neon-green);
        }

        .brief-meta {
            display: flex;
            align-items: center;
            gap: var(--sp-3);
            font-size: 0.72em;
            color: var(--text-muted);
            flex-wrap: wrap;
        }

        .brief-source {
            font-family: var(--font-mono);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            padding: 2px 8px;
            border-radius: var(--radius-sm);
            border: 1px solid var(--border);
            background: rgba(0, 0, 0, 0.20);
        }

        .brief-priority-tag {
            font-family: var(--font-mono);
            font-size: 0.92em;
            font-weight: 700;
            padding: 1px 6px;
            border-radius: var(--radius-sm);
        }
        .brief-priority-tag.bull {
            color: var(--bull-green);
            background: var(--bull-green-bg);
            border: 1px solid var(--bull-green-border);
        }
        .brief-priority-tag.bear {
            color: var(--bear-red);
            background: var(--bear-red-bg);
            border: 1px solid var(--bear-red-border);
        }
        .brief-priority-tag.neutral {
            color: var(--neutral-amber);
            background: var(--neutral-amber-bg);
            border: 1px solid var(--neutral-amber-border);
        }

        .brief-tags {
            display: flex;
            gap: var(--sp-1);
            flex-wrap: wrap;
        }

        .tag {
            font-family: var(--font-mono);
            font-size: 0.88em;
            background: rgba(0, 255, 136, 0.04);
            color: var(--text-muted);
            padding: 1px 8px;
            border-radius: var(--radius-sm);
            border: 1px solid var(--border);
            transition: border-color 0.2s;
        }

        .tag:hover {
            border-color: var(--neon-green);
            color: var(--neon-green);
        }

        .meta-sep {
            color: var(--text-dim);
        }

        /* ==========================================================
         * FOOTER — On-chain Address Style Monospace
         * ========================================================== */
        .footer {
            text-align: center;
            padding: var(--sp-10) var(--sp-6);
            margin-top: var(--sp-12);
            position: relative;
            background:
                repeating-linear-gradient(0deg, transparent, transparent 16px, rgba(0,255,136,0.015) 16px, rgba(0,255,136,0.015) 17px),
                repeating-linear-gradient(60deg, transparent, transparent 16px, rgba(98,126,234,0.012) 16px, rgba(98,126,234,0.012) 17px),
                repeating-linear-gradient(120deg, transparent, transparent 16px, rgba(247,147,26,0.010) 16px, rgba(247,147,26,0.010) 17px),
                linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
            border: 1px solid var(--border-medium);
            border-radius: var(--radius-xl);
            overflow: hidden;
        }

        /* Top multicolor neon line */
        .footer::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg,
                transparent 5%,
                var(--neon-green) 25%,
                var(--btc-orange) 50%,
                var(--eth-purple) 75%,
                transparent 95%);
            opacity: 0.6;
        }

        .footer-live {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-family: var(--font-mono);
            font-size: 0.65em;
            font-weight: 600;
            color: var(--neon-green);
            text-transform: uppercase;
            letter-spacing: 0.15em;
            margin-bottom: var(--sp-4);
        }

        .footer-brand {
            font-family: var(--font-mono);
            font-size: 0.70em;
            font-weight: 700;
            letter-spacing: 0.25em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: var(--sp-2);
        }

        .footer-stats {
            font-family: var(--font-mono);
            font-size: 1.6em;
            font-weight: 800;
            color: var(--text-bright);
            margin-bottom: var(--sp-4);
        }

        .footer-stats .stat-green { color: var(--neon-green); text-shadow: 0 0 12px var(--neon-green-glow); }

        .footer-line {
            width: 80px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-medium), transparent);
            margin: 0 auto var(--sp-4);
        }

        /* On-chain hash style */
        .footer-hash {
            font-family: var(--font-mono);
            font-size: 0.58em;
            color: var(--text-dim);
            letter-spacing: 0.04em;
            word-break: break-all;
            padding: var(--sp-3) var(--sp-4);
            background: rgba(0, 0, 0, 0.25);
            border: 1px solid var(--border);
            border-radius: var(--radius-md);
            margin: var(--sp-4) auto 0;
            max-width: 520px;
            line-height: 1.6;
        }

        .footer-hash .hash-label {
            color: var(--text-muted);
            display: block;
            margin-bottom: 2px;
        }

        .footer-hash .hash-value {
            color: var(--neon-green);
            opacity: 0.5;
        }

        .footer-sub {
            font-family: var(--font-mono);
            font-size: 0.62em;
            color: var(--text-dim);
            margin-top: var(--sp-3);
            letter-spacing: 0.05em;
        }

        /* ==========================================================
         * PRINT / PDF
         * ========================================================== */
        @media print {
            body {
                background: var(--bg-primary) !important;
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }
            .container { max-width: 100%; padding: 0; }
            .header {
                padding: var(--sp-6);
                margin-bottom: var(--sp-6);
            }
            .header::after { display: none; }
            .exec-summary { margin-bottom: var(--sp-6); }
            .section { margin-bottom: var(--sp-6); }
            .brief {
                padding: var(--sp-3) var(--sp-4);
            }
            .brief:hover { transform: none; background: var(--bg-card); }
            .brief.alert-critical { animation: none; }
            .footer { margin-top: var(--sp-8); }
            a { color: var(--text-primary) !important; }
            *, *::before, *::after {
                animation-duration: 0s !important;
                transition-duration: 0s !important;
            }
        }

        /* ==========================================================
         * MOBILE
         * ========================================================== */
        @media (max-width: 640px) {
            .container { padding: var(--sp-6) var(--sp-3); }
            .header {
                padding: var(--sp-8) var(--sp-4);
                border-radius: var(--radius-lg);
            }
            .header h1 { font-size: 1.8em; }
            .ticker-stats { grid-template-columns: repeat(2, 1fr); }
            .ticker-stat .stat-value { font-size: 1.4em; }
            .exec-body { padding: var(--sp-4); }
            .section-header {
                padding: var(--sp-2) var(--sp-4);
                border-radius: var(--radius-md);
            }
            .section-header h2 { font-size: 1.0em; }
            .section-emoji { width: 30px; height: 30px; font-size: 1.0em; }
            .brief {
                padding: var(--sp-3) var(--sp-3);
                gap: var(--sp-2);
                border-radius: var(--radius-md);
            }
            .brief-headline { font-size: 0.92em; }
            .footer {
                padding: var(--sp-6) var(--sp-4);
                border-radius: var(--radius-lg);
            }
            .footer-hash { font-size: 0.52em; }
        }

        @media (max-width: 380px) {
            body { font-size: 14px; }
            .header h1 { font-size: 1.5em; }
            .ticker-stats { grid-template-columns: 1fr 1fr; gap: var(--sp-2); }
            .ticker-stat { padding: var(--sp-2); }
        }

        @media (prefers-reduced-motion: reduce) {
            *, *::before, *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }
    </style>
</head>
<body>
    <div class="container">

        <!-- ====== HEADER: Exchange Ticker Board ====== -->
        <header class="header">
            <div class="header-top-bar">
                <span class="live-badge">
                    <span class="online-dot"></span>
                    LIVE FEED
                </span>
                <span class="session-id">SESSION {{ generated_time | replace('-','') | replace(' ','T') | replace(':','') }}Z</span>
            </div>

            <div class="header-brand">
                <span class="accent-green">[</span>NEWSLOOM<span class="accent-green">]</span>
                <span class="accent-orange">TERMINAL</span>
            </div>

            <h1>Crypto Intelligence<br>Terminal</h1>
            <div class="subtitle">// real-time signal aggregation · {{ date_str }}</div>

            <!-- Ticker Stats Row -->
            {% set ns = namespace(must_count=0, sections_active=0) %}
            {% for section_key in section_order %}
                {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
                    {% set ns.sections_active = ns.sections_active + 1 %}
                    {% for item in briefs[section_key] %}
                        {% if item.priority == '🔴' %}
                            {% set ns.must_count = ns.must_count + 1 %}
                        {% endif %}
                    {% endfor %}
                {% endif %}
            {% endfor %}

            <div class="ticker-stats">
                <div class="ticker-stat">
                    <div class="stat-label">SIGNALS</div>
                    <div class="stat-value green">{{ total_items }}</div>
                    <div class="stat-sub">total tracked</div>
                </div>
                <div class="ticker-stat">
                    <div class="stat-label">CRITICAL</div>
                    <div class="stat-value orange">{{ ns.must_count }}</div>
                    <div class="stat-sub">🔴 alerts</div>
                </div>
                <div class="ticker-stat">
                    <div class="stat-label">CHANNELS</div>
                    <div class="stat-value purple">{{ ns.sections_active }}</div>
                    <div class="stat-sub">active feeds</div>
                </div>
                <div class="ticker-stat">
                    <div class="stat-label">SOURCES</div>
                    <div class="stat-value white">
                        {% if stats and stats.total_input is defined %}{{ stats.total_input }}{% else %}∞{% endif %}
                    </div>
                    <div class="stat-sub">raw inputs</div>
                </div>
            </div>
        </header>

        <!-- ====== EXECUTIVE SUMMARY: Terminal Output ====== -->
        {% if executive_summary %}
        <section class="exec-summary">
            <div class="exec-titlebar">
                <div class="dots">
                    <span class="dot red"></span>
                    <span class="dot yellow"></span>
                    <span class="dot green"></span>
                </div>
                <span class="title">executive_summary.stdout</span>
                <span class="online-indicator">
                    <span class="online-dot"></span>
                    LIVE
                </span>
            </div>
            <div class="exec-body">
                <div class="exec-prompt">
                    <span style="color: var(--btc-orange);">newsloom</span><span style="color: var(--text-dim);">@</span><span style="color: var(--eth-purple);">terminal</span><span style="color: var(--text-dim);">:</span><span style="color: var(--neon-green);">~$</span> cat /signals/executive_summary<span class="cursor"></span>
                </div>
                {% for line in executive_summary.strip().split('\n') %}
                    {% if line.strip() %}
                <p>{{ line.strip() | md_inline }}</p>
                    {% endif %}
                {% endfor %}
            </div>
        </section>
        {% endif %}

        <!-- ====== SECTIONS ====== -->
        {% set global_ns = namespace(item_counter=0) %}
        {% for section_key in section_order %}
            {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
                {% set meta = section_configs.get(section_key, {}) %}
                {% set section_briefs = briefs[section_key] %}
                {% set section_color = meta.get('color', '#00FF88') %}

                {% if not loop.first %}
                <div class="circuit-separator">
                    <span class="node-dot" style="background: {{ section_color }}; box-shadow: 0 0 8px {{ section_color }};"></span>
                    <span class="line-segment"></span>
                    <span class="node-dot" style="background: {{ section_color }}; box-shadow: 0 0 8px {{ section_color }}; animation-delay: 0.5s;"></span>
                </div>
                {% endif %}

                <section class="section">
                    <header class="section-header">
                        <div class="color-bar" style="background: linear-gradient(180deg, {{ section_color }}, {{ section_color }}88);
                            box-shadow: 0 0 10px {{ section_color }}40;"></div>
                        <span class="section-emoji">{{ meta.get('emoji', '📌') }}</span>
                        <h2>{{ meta.get('title', section_key|title) }}</h2>
                        <span class="online-badge">
                            <span class="online-dot" style="background: {{ section_color }}; color: {{ section_color }};"></span>
                        </span>
                        <span class="section-count" style="color: {{ section_color }}; border-color: {{ section_color }}44;">
                            {{ section_briefs|length }} signals
                        </span>
                    </header>

                    <div class="briefs-grid">
                        {% for brief in section_briefs %}
                            {% set global_ns.item_counter = global_ns.item_counter + 1 %}
                            {% set alert_class = 'alert-critical' if brief.priority == '🔴' else ('alert-watch' if brief.priority == '🟡' else 'alert-normal') %}

                            <article class="brief {{ alert_class }}" style="{% if alert_class == 'alert-normal' %}border-left-color: {{ section_color }}22;{% endif %}">
                                <span class="brief-neon-stripe" style="{% if alert_class == 'alert-normal' %}background: linear-gradient(180deg, {{ section_color }}, {{ section_color }}88); opacity: 0.4;{% endif %}"></span>
                                <div class="brief-content">
                                    <div class="brief-headline">
                                        <a href="{{ brief.url }}" target="_blank">{{ brief.headline }}</a>
                                        {% if brief.priority == '🔴' %}
                                        <span class="alert-badge"><span class="blink-dot"></span> CRITICAL</span>
                                        {% endif %}
                                    </div>
                                    {% if brief.detail %}
                                    <div class="brief-detail">
                                        {{ brief.detail | md_inline }}
                                    </div>
                                    {% endif %}
                                    <footer class="brief-meta">
                                        <span class="brief-source" style="color: {{ section_color }}; border-color: {{ section_color }}33; background: {{ section_color }}0D;">{{ brief.source }}</span>
                                        {% if brief.priority == '🔴' %}
                                            <span class="brief-priority-tag bear">▼ ALERT</span>
                                        {% elif brief.priority == '🟡' %}
                                            <span class="brief-priority-tag neutral">◆ WATCH</span>
                                        {% else %}
                                            <span class="brief-priority-tag bull">▲ NORMAL</span>
                                        {% endif %}
                                        {% if brief.tags %}
                                        <span class="meta-sep">│</span>
                                        <div class="brief-tags">
                                            {% for tag in brief.tags %}
                                            <span class="tag">{{ tag }}</span>
                                            {% endfor %}
                                        </div>
                                        {% endif %}
                                    </footer>
                                </div>
                            </article>
                        {% endfor %}
                    </div>
                </section>
            {% endif %}
        {% endfor %}

        <!-- ====== FOOTER: On-Chain Address Style ====== -->
        <footer class="footer">
            <div class="footer-live">
                <span class="online-dot"></span>
                TERMINAL ACTIVE
            </div>
            <div class="footer-brand">[NEWSLOOM] CRYPTO TERMINAL</div>
            <div class="footer-stats">
                <span class="stat-green">{{ total_items }}</span> signals processed
            </div>
            <div class="footer-line"></div>

            <div class="footer-hash">
                <span class="hash-label">// session.proof</span>
                <span class="hash-value">0x{{ date_str | replace('-','') }}NEWSLOOM{{ total_items }}FEED{% for s in section_order %}{% if s in briefs and briefs[s]|length > 0 %}{{ s[:2]|upper }}{{ briefs[s]|length }}{% endif %}{% endfor %}CAFE</span>
            </div>

            <div class="footer-sub">
                Generated {{ generated_time }} · Powered by Newsloom · Crypto Neon v2.0
            </div>
        </footer>

    </div>
</body>
</html>