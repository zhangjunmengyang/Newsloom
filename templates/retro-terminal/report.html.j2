<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TERMINAL.EXE - NEWSLOOM v{{ date_str }}</title>
    <style>
        /* ============================================================
         * RETRO TERMINAL — CRT Phosphor Green Terminal
         * ============================================================
         * Deep green-black (#0a0f0a) background · Phosphor green text
         * CRT vignette · Scanlines · Amber highlights for must-read
         * BIOS boot header · Command-line footer with blinking cursor
         * Screen flicker · Box-drawing characters
         * Inspired by: VT100, Apple II, IBM 3270, Fallout terminals
         * ============================================================ */

        /* --- Design Tokens (CSS Custom Properties) --- */
        :root {
            /* Backgrounds — deep green-black, NOT pure black */
            --bg-terminal:      #0a0f0a;
            --bg-screen:        #0d140d;
            --bg-card:          #101a10;
            --bg-surface:       #142014;
            --bg-hover:         #1a2a1a;

            /* Text — phosphor green hierarchy */
            --text-primary:     #00FF41;
            --text-secondary:   #00CC33;
            --text-muted:       #009926;
            --text-dim:         #006619;

            /* Amber mode — for must-read highlights */
            --amber:            #FFB000;
            --amber-bright:     #FFC940;
            --amber-dim:        rgba(255, 176, 0, 0.25);
            --amber-glow:       rgba(255, 176, 0, 0.12);
            --amber-soft:       rgba(255, 176, 0, 0.06);

            /* Brand — phosphor green 5 variants */
            --phosphor:         #00FF41;
            --phosphor-bright:  #33FF66;
            --phosphor-dark:    #00AA2A;
            --phosphor-dim:     rgba(0, 255, 65, 0.25);
            --phosphor-glow:    rgba(0, 255, 65, 0.08);

            /* Priority — terminal alert colors */
            --priority-must:    #FFB000;
            --priority-must-bg: rgba(255, 176, 0, 0.06);
            --priority-rec:     #FFFF33;
            --priority-rec-bg:  rgba(255, 255, 51, 0.04);
            --priority-fyi:     #33FF33;
            --priority-fyi-bg:  rgba(51, 255, 51, 0.03);

            /* Borders — 3-level */
            --border:           rgba(0, 255, 65, 0.12);
            --border-strong:    rgba(0, 255, 65, 0.25);
            --border-accent:    rgba(0, 255, 65, 0.4);

            /* Spacing — 8pt grid */
            --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;
            --sp-4: 16px; --sp-5: 20px; --sp-6: 24px;
            --sp-8: 32px; --sp-10: 40px; --sp-12: 48px;
            --sp-16: 64px;

            /* Typography — Monospace + CJK fallback */
            --font-mono: "Courier New", "SF Mono", "Monaco", "Consolas", "Liberation Mono", "Noto Sans SC", monospace;
            --font-display: "VT323", "Courier New", "SF Mono", monospace;
            --font-cjk: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;

            /* Radius — sharp terminal aesthetic */
            --radius-sm: 0px;
            --radius-md: 2px;
            --radius-lg: 4px;

            /* Shadows — CRT glow effects */
            --shadow-card: 0 0 8px rgba(0, 255, 65, 0.06), inset 0 0 4px rgba(0, 255, 65, 0.03);
            --shadow-elevated: 0 0 16px rgba(0, 255, 65, 0.12), 0 0 4px rgba(0, 255, 65, 0.08);
            --shadow-glow: 0 0 24px rgba(0, 255, 65, 0.15), 0 0 48px rgba(0, 255, 65, 0.05);

            /* CRT effect controls */
            --scanline-opacity: 0.04;
            --glow-radius: 2px;
        }

        /* --- Page (PDF) --- */
        @page {
            size: A4;
            margin: 1cm 1.2cm;
            background: var(--bg-terminal);
            @bottom-right {
                content: "TERMINAL.EXE | {{ date_str }} | PAGE " counter(page);
                font-family: var(--font-mono);
                font-size: 6pt;
                color: var(--text-muted);
            }
        }

        /* --- Reset & Base --- */
        *, *::before, *::after {
            margin: 0; padding: 0; box-sizing: border-box;
        }

        body {
            font-family: var(--font-mono);
            background: var(--bg-terminal);
            color: var(--text-primary);
            font-size: 13px;
            line-height: 1.65;
            -webkit-font-smoothing: auto;
            -moz-osx-font-smoothing: auto;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
            cursor: text;
            overflow-x: hidden;
        }

        /* --- CRT Screen Container --- */
        .crt-screen {
            position: relative;
            min-height: 100vh;
            padding: var(--sp-10) var(--sp-8);
            /* CRT ambient inner glow */
            box-shadow:
                inset 0 0 60px rgba(0, 255, 65, 0.04),
                inset 0 0 120px rgba(0, 255, 65, 0.02),
                0 0 80px rgba(0, 255, 65, 0.03);
        }

        /* --- Scanlines overlay — subtle 2px repeating lines --- */
        .crt-screen::before {
            content: '';
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: repeating-linear-gradient(
                0deg,
                transparent,
                transparent 1px,
                rgba(0, 255, 65, var(--scanline-opacity)) 1px,
                rgba(0, 255, 65, var(--scanline-opacity)) 2px
            );
            pointer-events: none;
            z-index: 1000;
            animation: scanline-drift 10s linear infinite;
        }

        @keyframes scanline-drift {
            0% { transform: translateY(0); }
            100% { transform: translateY(4px); }
        }

        /* --- CRT Vignette — dark corners, bright center --- */
        .crt-screen::after {
            content: '';
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: radial-gradient(
                ellipse at center,
                transparent 55%,
                rgba(0, 0, 0, 0.15) 70%,
                rgba(0, 0, 0, 0.4) 85%,
                rgba(0, 0, 0, 0.65) 100%
            );
            pointer-events: none;
            z-index: 999;
        }

        /* --- Screen flicker — very low frequency, barely visible --- */
        @keyframes screen-flicker {
            0%, 97%, 100% { opacity: 1; }
            97.5% { opacity: 0.92; }
            98% { opacity: 0.97; }
            98.5% { opacity: 0.94; }
            99% { opacity: 1; }
        }

        .crt-screen {
            animation: screen-flicker 8s ease-in-out infinite;
        }

        /* --- Phosphor text glow utility --- */
        .glow-text {
            text-shadow:
                0 0 var(--glow-radius) currentColor,
                0 0 calc(var(--glow-radius) * 3) currentColor,
                0 0 calc(var(--glow-radius) * 6) rgba(0, 255, 65, 0.15);
        }

        /* --- Amber glow for must-read items --- */
        .glow-amber {
            text-shadow:
                0 0 2px var(--amber),
                0 0 6px var(--amber),
                0 0 12px rgba(255, 176, 0, 0.2);
        }

        /* --- Blinking cursor animation --- */
        @keyframes cursor-blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0; }
        }

        .cursor {
            background: var(--amber);
            color: var(--bg-terminal);
            animation: cursor-blink 1s step-end infinite;
            padding: 0 2px;
        }

        /* --- Pulse animation for status indicators --- */
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* --- Amber pulse for critical items --- */
        @keyframes amber-pulse {
            0%, 100% { opacity: 1; text-shadow: 0 0 4px var(--amber); }
            50% { opacity: 0.7; text-shadow: 0 0 8px var(--amber), 0 0 16px rgba(255, 176, 0, 0.3); }
        }

        /* --- Link styles --- */
        a {
            color: var(--phosphor-bright);
            text-decoration: underline;
            text-decoration-style: dotted;
            text-underline-offset: 3px;
            transition: all 0.15s ease;
        }

        a:hover {
            color: var(--amber);
            text-shadow: 0 0 6px currentColor;
            text-decoration-style: solid;
        }

        /* --- Terminal Container --- */
        .terminal {
            max-width: 96ch;
            margin: 0 auto;
            position: relative;
            z-index: 2;
        }

        /* ============================================================
         * BIOS BOOT HEADER — Looks like actual BIOS POST screen
         * ============================================================ */
        .bios-header {
            margin-bottom: var(--sp-6);
            color: var(--text-secondary);
            font-size: 0.85em;
            line-height: 1.6;
        }

        .bios-brand {
            color: var(--amber-bright);
            font-weight: bold;
            letter-spacing: 0.1em;
            margin-bottom: var(--sp-2);
        }

        .bios-version {
            color: var(--text-muted);
            margin-bottom: var(--sp-3);
        }

        .bios-line {
            color: var(--text-secondary);
            padding-left: 0;
        }

        .bios-line.pass {
            color: var(--phosphor);
        }

        .bios-line.pass::after {
            content: ' .......... OK';
            color: var(--text-muted);
        }

        .bios-line.mem {
            color: var(--amber);
        }

        .bios-datestamp {
            color: var(--text-dim);
            border-top: 1px solid var(--border);
            padding-top: var(--sp-2);
            margin-top: var(--sp-3);
            font-size: 0.9em;
        }

        /* --- ASCII art header decoration --- */
        .ascii-header {
            color: var(--text-muted);
            text-align: center;
            margin-bottom: var(--sp-4);
            white-space: pre;
            font-size: 0.7em;
            line-height: 1.15;
            letter-spacing: 0.05em;
        }

        /* ============================================================
         * BOOT SEQUENCE — System initialization display
         * ============================================================ */
        .boot-sequence {
            margin-bottom: var(--sp-12);
            border: 1px solid var(--border);
            padding: var(--sp-6);
            background: var(--bg-screen);
            position: relative;
        }

        .boot-sequence::before {
            content: '┌─── SYSTEM BOOT LOG ───┐';
            position: absolute;
            top: -10px; left: var(--sp-4);
            background: var(--bg-terminal);
            padding: 0 var(--sp-2);
            color: var(--text-muted);
            font-size: 0.85em;
        }

        .boot-line {
            margin-bottom: var(--sp-1);
            color: var(--text-secondary);
            padding-left: var(--sp-4);
            position: relative;
            line-height: 1.8;
        }

        .boot-line::before {
            content: '>';
            position: absolute;
            left: 0;
            color: var(--text-muted);
        }

        .boot-line.success {
            color: var(--priority-fyi);
        }

        .boot-line.success::after {
            content: ' ✓';
            color: var(--priority-fyi);
        }

        .boot-line.warning {
            color: var(--amber);
        }

        .boot-line.warning::after {
            content: ' ⚠';
        }

        .boot-line.error {
            color: #FF3333;
            font-weight: bold;
        }

        .boot-line.error::after {
            content: ' ✗';
        }

        /* ============================================================
         * SYSTEM INFO — Status panel with box-drawing characters
         * ============================================================ */
        .system-info {
            border: 1px solid var(--border-strong);
            padding: var(--sp-6);
            margin: var(--sp-8) 0;
            background:
                linear-gradient(180deg, var(--bg-screen) 0%, var(--bg-terminal) 100%);
            position: relative;
        }

        .system-info::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg,
                transparent 0%,
                var(--phosphor) 20%,
                var(--phosphor) 80%,
                transparent 100%
            );
            opacity: 0.5;
        }

        .system-title {
            color: var(--phosphor-bright);
            font-weight: bold;
            text-align: center;
            margin-bottom: var(--sp-6);
            font-size: 1.1em;
            letter-spacing: 0.15em;
        }

        .system-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(22ch, 1fr));
            gap: var(--sp-3);
            font-size: 0.9em;
        }

        .stat-line {
            display: flex;
            justify-content: space-between;
            padding: var(--sp-1) var(--sp-2);
            border-bottom: 1px dotted var(--border);
            transition: background 0.15s ease;
        }

        .stat-line:hover {
            background: var(--bg-hover);
        }

        .stat-line .label {
            color: var(--text-muted);
        }

        .stat-value {
            color: var(--phosphor-bright);
            font-weight: bold;
        }

        .stat-value.critical {
            color: var(--amber);
            animation: amber-pulse 2s infinite;
        }

        /* ============================================================
         * COMMAND PROMPT — Interactive prompt display
         * ============================================================ */
        .prompt {
            color: var(--phosphor-bright);
            margin-bottom: var(--sp-2);
            display: flex;
            align-items: center;
            gap: var(--sp-2);
            line-height: 1.8;
        }

        .prompt-user {
            color: var(--text-muted);
        }

        .prompt-path {
            color: var(--phosphor-dark);
        }

        .prompt-symbol {
            color: var(--amber);
            font-weight: bold;
        }

        .prompt-cmd {
            color: var(--phosphor-bright);
        }

        /* ============================================================
         * EXECUTIVE SUMMARY — System output block
         * ============================================================ */
        .system-output {
            border-left: 3px solid var(--border-strong);
            padding: var(--sp-6) var(--sp-6);
            margin: var(--sp-6) 0 var(--sp-10);
            background: linear-gradient(90deg, var(--phosphor-glow) 0%, transparent 60%);
            page-break-inside: avoid;
            position: relative;
        }

        .system-output::before {
            content: '/* OUTPUT START */';
            position: absolute;
            top: -8px; left: var(--sp-6);
            color: var(--text-dim);
            font-size: 0.75em;
            background: var(--bg-terminal);
            padding: 0 var(--sp-2);
        }

        .system-output::after {
            content: '/* OUTPUT END */';
            position: absolute;
            bottom: -8px; left: var(--sp-6);
            color: var(--text-dim);
            font-size: 0.75em;
            background: var(--bg-terminal);
            padding: 0 var(--sp-2);
        }

        .output-header {
            color: var(--phosphor-bright);
            margin-bottom: var(--sp-4);
            letter-spacing: 0.1em;
            font-size: 0.9em;
            text-transform: uppercase;
        }

        .output-content {
            color: var(--text-secondary);
            line-height: 1.75;
        }

        .output-content p {
            margin-bottom: var(--sp-4);
            padding-left: var(--sp-4);
            position: relative;
        }

        .output-content p::before {
            content: '│';
            position: absolute;
            left: 0;
            color: var(--text-dim);
        }

        .output-content p:last-child { margin-bottom: 0; }

        .output-content strong {
            color: var(--text-primary);
            font-weight: normal;
            text-decoration: underline;
            text-decoration-color: var(--phosphor-dark);
        }

        .output-content code {
            color: var(--phosphor-bright);
            background: rgba(0, 255, 65, 0.08);
            padding: 1px 4px;
            border: 1px solid var(--border);
        }

        /* ============================================================
         * SECTION — Command category with decorative borders
         * Each section uses its own color from section_configs
         * ============================================================ */
        .section {
            margin: var(--sp-12) 0;
        }

        .section-header {
            border: 1px solid var(--border-strong);
            border-bottom: 2px solid var(--phosphor-dark);
            padding: var(--sp-3) var(--sp-4);
            margin-bottom: var(--sp-6);
            background: linear-gradient(180deg, var(--bg-surface) 0%, var(--bg-screen) 100%);
            position: relative;
        }

        .section-header::before {
            content: '';
            position: absolute;
            top: 0; left: 0;
            width: 4px; height: 100%;
            background: var(--section-color, var(--phosphor));
            opacity: 0.8;
        }

        .section-title-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .section-title {
            color: var(--phosphor-bright);
            text-transform: uppercase;
            letter-spacing: 0.12em;
            font-size: 1.05em;
            font-weight: bold;
        }

        .section-count-badge {
            font-size: 0.8em;
            color: var(--text-muted);
            background: var(--bg-terminal);
            border: 1px solid var(--border);
            padding: var(--sp-1) var(--sp-3);
        }

        .section-info {
            display: flex;
            justify-content: space-between;
            font-size: 0.8em;
            color: var(--text-dim);
            margin-top: var(--sp-2);
            padding-top: var(--sp-2);
            border-top: 1px dotted var(--border);
        }

        /* Section color indicator dot */
        .section-color-dot {
            display: inline-block;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            margin-right: var(--sp-2);
            vertical-align: middle;
            box-shadow: 0 0 6px currentColor;
        }

        /* ============================================================
         * COMMAND ENTRY — Individual brief as terminal command
         * ============================================================ */
        .command-entry {
            margin-bottom: var(--sp-4);
            padding: var(--sp-4) var(--sp-4);
            page-break-inside: avoid;
            border: 1px solid transparent;
            transition: all 0.2s ease;
            position: relative;
        }

        .command-entry:hover {
            background: var(--bg-screen);
            border-color: var(--border);
        }

        /* --- Priority-specific: must-read uses AMBER mode --- */
        .command-entry.must-read {
            background: var(--amber-soft);
            border-left: 3px solid var(--amber);
            box-shadow: inset 0 0 30px rgba(255, 176, 0, 0.04);
        }

        .command-entry.must-read::before {
            content: '!!! AMBER ALERT !!!';
            position: absolute;
            top: 2px; right: var(--sp-3);
            font-size: 0.65em;
            color: var(--amber);
            animation: amber-pulse 1.5s infinite;
            letter-spacing: 0.1em;
        }

        .command-entry.recommended {
            border-left: 3px solid var(--priority-rec);
            background: var(--priority-rec-bg);
        }

        .command-entry.fyi {
            border-left: 3px solid var(--priority-fyi);
            background: var(--priority-fyi-bg);
        }

        /* --- Command line (headline row) --- */
        .command-line {
            display: flex;
            align-items: flex-start;
            gap: var(--sp-2);
            margin-bottom: var(--sp-2);
        }

        .command-prompt {
            flex-shrink: 0;
            min-width: 2ch;
            font-weight: bold;
        }

        .command-entry.must-read .command-prompt {
            color: var(--amber);
            text-shadow: 0 0 6px rgba(255, 176, 0, 0.5);
        }

        .command-entry.recommended .command-prompt {
            color: var(--priority-rec);
            text-shadow: 0 0 4px rgba(255, 255, 51, 0.3);
        }

        .command-entry.fyi .command-prompt {
            color: var(--priority-fyi);
        }

        .command-input {
            flex: 1;
            color: var(--text-primary);
            word-wrap: break-word;
            line-height: 1.5;
        }

        .command-input a {
            color: var(--phosphor-bright);
        }

        /* Must-read headlines in amber */
        .command-entry.must-read .command-input a {
            color: var(--amber-bright);
            font-weight: bold;
            text-shadow: 0 0 4px rgba(255, 176, 0, 0.3);
        }

        /* --- Command output (detail text) --- */
        .command-output {
            margin-left: calc(2ch + var(--sp-2));
            color: var(--text-secondary);
            line-height: 1.7;
            margin-bottom: var(--sp-3);
            padding-left: var(--sp-3);
            border-left: 1px solid var(--border);
        }

        .command-output strong {
            color: var(--text-primary);
            text-decoration: underline;
            text-decoration-color: var(--phosphor-dark);
        }

        .command-output code {
            color: var(--phosphor-bright);
            background: rgba(0, 255, 65, 0.06);
            padding: 1px 3px;
            border: 1px solid var(--border);
        }

        /* --- Command metadata row --- */
        .command-meta {
            margin-left: calc(2ch + var(--sp-2));
            font-size: 0.8em;
            color: var(--text-dim);
            display: flex;
            gap: var(--sp-4);
            flex-wrap: wrap;
            padding-top: var(--sp-2);
            border-top: 1px dotted var(--border);
        }

        .meta-item {
            display: flex;
            gap: var(--sp-1);
        }

        .meta-label {
            color: var(--text-muted);
        }

        .meta-value {
            color: var(--text-primary);
        }

        .meta-value.critical { color: var(--amber); }
        .meta-value.warning { color: var(--priority-rec); }
        .meta-value.normal { color: var(--priority-fyi); }

        /* --- Tags as [BRACKETED] items --- */
        .command-tags {
            margin-left: calc(2ch + var(--sp-2));
            margin-top: var(--sp-2);
            display: flex;
            flex-wrap: wrap;
            gap: var(--sp-2);
        }

        .command-tag {
            color: var(--text-muted);
            font-size: 0.8em;
            transition: color 0.15s ease;
        }

        .command-tag::before { content: '['; color: var(--phosphor-dark); }
        .command-tag::after { content: ']'; color: var(--phosphor-dark); }

        .command-tag:hover {
            color: var(--text-primary);
        }

        /* --- Separator between entries --- */
        .command-entry + .command-entry {
            border-top: 1px dotted var(--border);
        }

        /* ============================================================
         * PROGRESS BAR — Loading/status display
         * ============================================================ */
        .progress-bar {
            display: flex;
            align-items: center;
            gap: var(--sp-2);
            margin: var(--sp-4) 0;
            font-size: 0.85em;
        }

        .progress-bar .bar {
            flex: 1;
            height: 8px;
            background: var(--bg-card);
            border: 1px solid var(--border);
            position: relative;
            overflow: hidden;
        }

        .progress-bar .fill {
            height: 100%;
            background: var(--phosphor);
            box-shadow: 0 0 6px rgba(0, 255, 65, 0.4);
            transition: width 0.3s ease;
        }

        .progress-bar .pct {
            color: var(--phosphor-bright);
            min-width: 4ch;
            text-align: right;
        }

        /* ============================================================
         * DIVIDER — Decorative terminal separator
         * ============================================================ */
        .terminal-divider {
            text-align: center;
            color: var(--text-dim);
            margin: var(--sp-8) 0;
            font-size: 0.75em;
            letter-spacing: 0.3em;
            overflow: hidden;
        }

        .terminal-divider::before,
        .terminal-divider::after {
            content: '════════════════════════';
            color: var(--border-strong);
        }

        /* ============================================================
         * FOOTER — Command-line prompt with blinking cursor
         * root@newsloom:~# █
         * ============================================================ */
        .system-status {
            border: 1px solid var(--border-strong);
            margin-top: var(--sp-16);
            padding: var(--sp-8) var(--sp-6);
            background: linear-gradient(180deg, var(--bg-screen) 0%, var(--bg-terminal) 100%);
            position: relative;
        }

        .system-status::before {
            content: '';
            position: absolute;
            bottom: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg,
                transparent 0%,
                var(--phosphor) 30%,
                var(--phosphor) 70%,
                transparent 100%
            );
            opacity: 0.4;
        }

        .status-line {
            color: var(--text-muted);
            margin-bottom: var(--sp-2);
            letter-spacing: 0.05em;
        }

        .status-line.brand {
            color: var(--phosphor-bright);
            font-size: 1.05em;
            font-weight: bold;
            letter-spacing: 0.15em;
        }

        .status-ready {
            color: var(--priority-fyi);
            animation: pulse 2s infinite;
            font-size: 1.1em;
            margin: var(--sp-4) 0;
            letter-spacing: 0.2em;
        }

        .status-uptime {
            font-size: 0.75em;
            color: var(--text-dim);
            margin-top: var(--sp-4);
        }

        /* Footer command prompt — the signature "root@newsloom:~# █" */
        .footer-prompt {
            margin-top: var(--sp-6);
            padding-top: var(--sp-4);
            border-top: 1px solid var(--border);
            font-size: 1em;
        }

        .footer-prompt .user {
            color: var(--phosphor);
        }

        .footer-prompt .host {
            color: var(--phosphor);
        }

        .footer-prompt .at {
            color: var(--text-muted);
        }

        .footer-prompt .colon {
            color: var(--text-muted);
        }

        .footer-prompt .path {
            color: var(--amber);
        }

        .footer-prompt .hash {
            color: var(--text-muted);
        }

        .footer-prompt .blinking-cursor {
            display: inline-block;
            background: var(--phosphor);
            color: var(--bg-terminal);
            animation: cursor-blink 1s step-end infinite;
            padding: 0 1px;
            margin-left: 2px;
        }

        /* ============================================================
         * Print / PDF Overrides
         * ============================================================ */
        @media print {
            body {
                background: var(--bg-terminal) !important;
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }

            .crt-screen::before,
            .crt-screen::after {
                display: none !important;
            }

            .crt-screen {
                animation: none !important;
            }

            .terminal { max-width: 100%; }

            .boot-sequence {
                animation: none;
                overflow: visible;
                white-space: normal;
            }

            .cursor, .blinking-cursor { animation: none; }

            .command-entry.must-read::before { animation: none; }
            .status-ready { animation: none; }
            .stat-value.critical { animation: none; }

            .command-entry:hover { background: transparent; }
            .stat-line:hover { background: transparent; }

            a { color: var(--phosphor-bright) !important; }

            .section { margin-bottom: var(--sp-8); }
            .system-output { margin-bottom: var(--sp-6); }
        }

        /* ============================================================
         * Mobile Responsive
         * ============================================================ */
        @media (max-width: 640px) {
            body { font-size: 11px; }

            .crt-screen {
                padding: var(--sp-4) var(--sp-3);
            }

            .terminal { max-width: 100%; }

            .system-stats {
                grid-template-columns: 1fr;
            }

            .command-meta {
                flex-direction: column;
                gap: var(--sp-1);
            }

            .command-entry {
                padding: var(--sp-3);
            }

            .command-entry.must-read::before {
                display: none;
            }

            .section-info {
                flex-direction: column;
                gap: var(--sp-1);
            }

            .boot-sequence {
                padding: var(--sp-3);
            }

            .boot-sequence::before {
                font-size: 0.7em;
            }

            .bios-header {
                font-size: 0.75em;
            }
        }
    </style>
</head>
<body>
    <div class="crt-screen">
        <div class="terminal">

            <!-- ====== BIOS BOOT HEADER ====== -->
            <div class="bios-header">
                <div class="bios-brand">NEWSLOOM BIOS v3.2.1 — Award Modular BIOS v6.00PG</div>
                <div class="bios-version">Copyright (C) 2024-2026 Newsloom Systems, Inc.</div>
                <div class="bios-line pass">CPU: Neural-AI Processor @ 3.20GHz</div>
                <div class="bios-line mem">Memory Test: 131072K OK</div>
                <div class="bios-line pass">Primary Master: NEWSLOOM-DATA-DRIVE</div>
                <div class="bios-line pass">Primary Slave: ARCHIVE-STORAGE</div>
                <div class="bios-datestamp">{{ date_str }} {{ generated_time }} | Press DEL to enter SETUP</div>
            </div>

            <!-- ====== ASCII ART HEADER ====== -->
            <div class="ascii-header glow-text">
 ███╗   ██╗███████╗██╗    ██╗███████╗██╗      ██████╗  ██████╗ ███╗   ███╗
 ████╗  ██║██╔════╝██║    ██║██╔════╝██║     ██╔═══██╗██╔═══██╗████╗ ████║
 ██╔██╗ ██║█████╗  ██║ █╗ ██║███████╗██║     ██║   ██║██║   ██║██╔████╔██║
 ██║╚██╗██║██╔══╝  ██║███╗██║╚════██║██║     ██║   ██║██║   ██║██║╚██╔╝██║
 ██║ ╚████║███████╗╚███╔███╔╝███████║███████╗╚██████╔╝╚██████╔╝██║ ╚═╝ ██║
 ╚═╝  ╚═══╝╚══════╝ ╚══╝╚══╝ ╚══════╝╚══════╝ ╚═════╝  ╚═════╝ ╚═╝     ╚═╝
            </div>

            <!-- ====== BOOT SEQUENCE ====== -->
            <div class="boot-sequence">
                <div class="boot-line">NEWSLOOM TERMINAL v3.2 — DAILY INTELLIGENCE SYSTEM</div>
                <div class="boot-line">Initializing neural network subsystem...</div>
                <div class="boot-line success">AI inference core loaded [OK]</div>
                <div class="boot-line">Scanning {{ section_order|length }} data channels...</div>
                <div class="boot-line success">{{ total_items }} articles scraped and processed [OK]</div>
                <div class="boot-line">Running priority classification filters...</div>
                {% set must_read_total = [] %}
                {% for section_key, items in briefs.items() %}
                {% if items is iterable and items is not string %}
                {% for item in items %}
                {% if item.priority == '🔴' %}
                {% if must_read_total.append(1) %}{% endif %}
                {% endif %}
                {% endfor %}
                {% endif %}
                {% endfor %}
                <div class="boot-line {% if must_read_total|length > 0 %}warning{% else %}success{% endif %}">
                    Critical alerts detected: {{ must_read_total|length }} [{% if must_read_total|length > 0 %}AMBER ALERT{% else %}CLEAR{% endif %}]
                </div>
                <div class="boot-line success">System ready — {{ date_str }} {{ generated_time }}</div>
            </div>

            <!-- ====== SYSTEM INFO PANEL ====== -->
            <div class="system-info">
                <div class="system-title glow-text">╔══════════ SYSTEM STATUS ══════════╗</div>
                <div class="system-stats">
                    <div class="stat-line">
                        <span class="label">DATE:</span>
                        <span class="stat-value">{{ date_str }}</span>
                    </div>
                    <div class="stat-line">
                        <span class="label">TIME:</span>
                        <span class="stat-value">{{ generated_time }}</span>
                    </div>
                    <div class="stat-line">
                        <span class="label">TOTAL_ITEMS:</span>
                        <span class="stat-value">{{ total_items }}</span>
                    </div>
                    <div class="stat-line">
                        <span class="label">CRITICAL:</span>
                        <span class="stat-value {% if must_read_total|length > 0 %}critical{% endif %}">{{ must_read_total|length }}</span>
                    </div>
                    <div class="stat-line">
                        <span class="label">CATEGORIES:</span>
                        <span class="stat-value">{{ section_order|length }}</span>
                    </div>
                    <div class="stat-line">
                        <span class="label">STATUS:</span>
                        <span class="stat-value">ACTIVE</span>
                    </div>
                </div>
            </div>

            <!-- ====== EXECUTIVE SUMMARY: System Output ====== -->
            {% if executive_summary %}
            <div class="prompt glow-text">
                <span class="prompt-user">root@newsloom</span>
                <span class="prompt-path">~/intel</span>
                <span class="prompt-symbol">$</span>
                <span class="prompt-cmd">cat executive_summary.log</span>
            </div>
            <div class="system-output">
                <div class="output-header glow-text">━━ EXECUTIVE BRIEFING OUTPUT ━━</div>
                <div class="output-content">
                    {% for line in executive_summary.strip().split('\n') %}
                    {% if line.strip() %}
                    <p>{{ line.strip() | md_inline }}</p>
                    {% endif %}
                    {% endfor %}
                </div>
            </div>
            <div class="prompt">
                <span class="prompt-user">root@newsloom</span>
                <span class="prompt-path">~/intel</span>
                <span class="prompt-symbol">$</span>
                <span class="cursor">█</span>
            </div>
            {% endif %}

            <div class="terminal-divider">═══ CLASSIFIED DATA STREAMS ═══</div>

            <!-- ====== SECTIONS: Command Categories ====== -->
            {% for section_key in section_order %}
            {% if section_key in briefs and briefs[section_key] is iterable and briefs[section_key] is not string and briefs[section_key]|length > 0 %}
            {% set meta = section_configs.get(section_key, {}) %}
            {% set items = briefs[section_key] %}
            {% set sec_color = meta.color|default('#00FF41') %}
            <section class="section" style="--section-color: {{ sec_color }};">
                <div class="section-header" style="border-bottom-color: {{ sec_color }}40;">
                    <div class="section-title-row">
                        <div class="section-title glow-text">
                            <span class="section-color-dot" style="background: {{ sec_color }}; color: {{ sec_color }};"></span>
                            {{ meta.emoji|default('$') }} {{ meta.title|default(section_key)|upper }}
                        </div>
                        <span class="section-count-badge">{{ items|length }} REC</span>
                    </div>
                    <div class="section-info">
                        <span>CHANNEL: {{ section_key|upper }}</span>
                        <span>SCANNED: {{ generated_time }}</span>
                    </div>
                </div>

                {% for item in items %}
                {% set priority = item.priority|default('🟢') %}
                {% set p_class = 'must-read' if priority == '🔴' else ('recommended' if priority == '🟡' else 'fyi') %}
                <div class="command-entry {{ p_class }}">
                    <div class="command-line">
                        <span class="command-prompt glow-text">&gt;</span>
                        <div class="command-input">
                            <a href="{{ item.url|default('#') }}" target="_blank">{{ item.headline|default(item.title|default('UNTITLED.TXT')) }}</a>
                        </div>
                    </div>

                    {% if item.detail %}
                    <div class="command-output">{{ item.detail|default('') | md_inline }}</div>
                    {% endif %}

                    <div class="command-meta">
                        {% if item.source %}
                        <div class="meta-item">
                            <span class="meta-label">SRC:</span>
                            <span class="meta-value">{{ item.source|upper }}</span>
                        </div>
                        {% endif %}
                        <div class="meta-item">
                            <span class="meta-label">PRI:</span>
                            <span class="meta-value {% if priority == '🔴' %}critical{% elif priority == '🟡' %}warning{% else %}normal{% endif %}">
                                {% if priority == '🔴' %}AMBER/CRITICAL
                                {% elif priority == '🟡' %}ELEVATED
                                {% else %}STANDARD{% endif %}
                            </span>
                        </div>
                        <div class="meta-item">
                            <span class="meta-label">TS:</span>
                            <span class="meta-value">{{ generated_time }}</span>
                        </div>
                    </div>

                    {% if item.tags %}
                    <div class="command-tags">
                        {% for tag in item.tags %}
                        <span class="command-tag">{{ tag|upper }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
                {% endfor %}
            </section>
            {% endif %}
            {% endfor %}

            <!-- ====== SYSTEM STATUS (Footer) ====== -->
            <div class="system-status">
                <div class="status-line">═══════════════════════════════════════════════════════════════</div>
                <div class="status-line brand glow-text">NEWSLOOM TERMINAL INTELLIGENCE SYSTEM</div>
                <div class="status-line">SESSION: {{ date_str }} · {{ total_items }} RECORDS PROCESSED</div>
                <div class="status-line">CHANNELS: {{ section_order|length }} · CRITICAL: {{ must_read_total|length }}</div>
                <div class="status-ready glow-text">█ SYSTEM READY █</div>
                <div class="status-uptime">UPTIME: 24h 00m · MEM: 128MB / 131072K · CPU: 12% · NET: OK</div>

                <!-- The signature command-line footer -->
                <div class="footer-prompt">
                    <span class="user">root</span><span class="at">@</span><span class="host">newsloom</span><span class="colon">:</span><span class="path">~</span><span class="hash"># </span><span class="blinking-cursor">█</span>
                </div>
            </div>

        </div>
    </div>
</body>
</html>
